# Decisions

Append-only log of architecture and collaboration decisions, shared between both founders. Newest at the bottom. Record the *why*, not just the *what*. Product and strategy decisions live in the Google Doc; this file is for how we build and work.

## 2026-06-19

- **`AGENTS.md` is the single source of truth for agents.** Codex reads `AGENTS.md` natively and `CLAUDE.md` only imports it, so both agents and both founders share one contract instead of two that drift. *Why:* we work with multiple agents (Claude Code, Codex) and a second founder; divergent instructions are how agents end up working blind.
- **Keep Aymane's scaffold as the technical base; rebuild the core mechanic on top.** The scaffold (signup, global directory, open DM) is kept for its working Supabase wiring, but the discreet-like + match-gate mechanic is built over it rather than starting from scratch. *Why:* the wiring is reusable; the open-DM flow violates the discreet double opt-in invariant and cannot ship as is.
- **Phase 1 build order: Bloc 0 (foundations), then 2 (like/match), then 1 (presence), then 3 (chat gate), then 4 (safety).** *Why:* the like/match mechanic is the hypothesis the whole product rests on, so it comes right after the data model it depends on.
- **Repo language is English, conversation can be French.** Code, comments, docs and commits are English; talking to agents in French is fine. *Why:* durable artifacts must be readable by both founders and any future contributor.
- **`docs/` holds the shared roadmap and decision log.** `docs/roadmap.md` for status and plan, `docs/decisions.md` for decisions. *Why:* Todoist is personal to Marwane and Aymane cannot see it; the shared trace must live in the repo.
- **V1 auth: Supabase anonymous sign-in, no account creation at the door.** Scanning the QR triggers `signInAnonymously()` automatically, so every visitor gets a real `auth.users` row (UUID + JWT session) with zero friction — no email, no SMS, no password. *Why:* the product is scan → see who's here, right now; a signup wall at a noisy bar kills the funnel. An anonymous session still gives us `auth.uid()`, so RLS is fully enforceable from day one (security-first) — which "no auth at all" cannot do.
- **Identity model: ephemeral presence and matches, persistent profile.** What persists is the profile (so a regular doesn't rebuild it every night). What is ephemeral is presence (fades on leave/timeout) **and the match/chat** (dies with the night). *Why:* the ephemerality of the match is the forcing function — you matched, you have *tonight* to go talk IRL. Persisting matches would reintroduce the async pen-pal dynamic the north star rejects (in-person first). We never promise users they can "retrieve their matches later".
- **Profile = first name + photo (required) + bio (optional). Email is optional and collected later, never at entry.** The required photo + first name is the soft identity / women-first layer (you see a real face), not a verified email. Email is offered after onboarding, framed as "get notified about the next live night" — it triggers the anonymous→permanent account upgrade (same UUID, profile becomes cross-device recoverable). *Why:* email earns us a re-engagement channel and the only real returning-user retention metric, but framed around bringing people back to a *live* room, not maintaining async chats. Email is PII: it must never be exposed to other users (RLS, no `select("*")`), and storing it touches the open RGPD / Supabase-region (EU vs US) question to settle with Aymane before any public test.
- **PII lives in a separate `profile_private` table, not in `profiles`.** `profiles` holds only public columns (first name, photo, bio, gender, interested_in); email/phone go in `profile_private` with owner-only RLS on every operation. *Why:* RLS is row-level and cannot hide a column of a row that is otherwise readable. Isolating PII in its own table is the only hard guarantee email never leaks — it does not depend on every future query remembering to avoid `select("*")`.
- **Discreet like enforced in RLS, not just the UI.** `likes` SELECT is `liker_id = auth.uid()`: you can read only the likes you sent, never one targeting you. The only signal that surfaces is a `matches` row, and only when mutual. *Why:* invariant 3 (no one is ever told they were liked) must hold at the database, so a bug or a direct API call can't leak "who liked me".
- **Matches are created by a `SECURITY DEFINER` trigger on `likes`, never by users.** `handle_new_like()` checks for the reciprocal like in the same venue and inserts the ordered pair into `matches`; EXECUTE is revoked from the API roles. *Why:* the reciprocity check must read `likes` rows the user is not allowed to SELECT, so it runs as definer; keeping inserts out of users' hands means the match is the single trustworthy mutual signal.
- **Messages are tied to a `match_id` (no open DM possible).** A message references a match and RLS requires the sender to be one of its two members. *Why:* makes the "no open messaging" red line structural — a message literally cannot exist without a prior mutual match.
- **Profile carries `gender` + `interested_in` (set of woman/man/nonbinary) from the start.** *Why:* `interested_in` is meaningless without the other person's `gender` to filter on, so both ship together rather than bolting gender on later. (`interested_in` is an array to allow more than one.)
- **Likes and matches are venue-scoped; ephemerality (expiry/cleanup) is deferred to Bloc 1/3.** Foundations ship the tables with no TTL yet; the "match dies with the night" cleanup job is a later bloc (noted in roadmap so it isn't forgotten). *Why:* keep Bloc 0 to the data model; the forcing-function expiry needs the presence timeout logic that lands with Bloc 1.
- **`NEXT_PUBLIC_SUPABASE_ANON_KEY` renamed to `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`.** The client now uses the new `sb_publishable_...` key, and `lib/supabase.ts` is typed with the generated `Database` type (`lib/database.types.ts`). *Why:* Supabase's new publishable key is the recommended browser key; the var name now matches reality.
- **Presence model: "the room of tonight", not a minute-by-minute timeout.** You are present from check-in until you explicitly leave or the night rolls over at a fixed local hour. There is **no short heartbeat timeout that hides you**. *Why:* a heartbeat only fires while the tab is open and active; at a bar the phone is in a pocket, screen locked. A short timeout would drop people who are physically in the room, and we cannot distinguish "phone in pocket, still here" from "went home" — both just stop heart-beating. A short window also empties the room and kills the cold-start. The "live" invariant is honoured at the granularity of *tonight*, which is the right grain for a bar. Risk accepted: you might match with someone who already left — but the match is ephemeral and the promise is "they're here tonight", same as real life.
- **The night ends at 06:00 local time, enforced by a `pg_cron` job (`bartap-close-ended-nights`, every 15 min).** `close_ended_nights()` sets `left_at` on every active presence past the first 06:00 (in the venue's timezone) strictly after its check-in — handles after-midnight check-ins (02:00 belongs to the night that ends that same morning). Venues carry an explicit `timezone` column (Paris → `Europe/Paris`, NYC → `America/New_York`) so the rollover does not hardcode a city map. *Why:* this cron, not a client timeout, is what empties the room for good; 06:00 maps to a bar closing and is predictable.
- **`check_in(venue)` RPC is the single check-in path: one room at a time, idempotent, also the heartbeat.** It leaves any active presence in a *different* venue, then bumps `last_seen_at` if already here or inserts a fresh row. `SECURITY INVOKER` so it runs under the caller's own RLS. *Why:* a clean atomic upsert against the partial unique index (`one active presence per profile`) without client-side juggling; reusing it as the heartbeat keeps one code path.
- **Explicit "leave the room" control ships in Bloc 1, not deferred to Bloc 4.** A button sets `left_at` and removes you from the room immediately. *Why:* it is cheap, it is the natural complement to the night-session model (the only way to disappear before the rollover), and it is a first concrete women-first control over your own visibility.
- **`profiles` and `presence` SELECT tightened to co-present users now that presence exists.** `profiles` is readable only for yourself, anyone co-present in a venue you are currently in, or anyone you have matched with (so a match stays readable after they leave); `presence` only for your own rows and the venues you are in. This closes the intentional Bloc 0 hole (profiles readable by any authenticated session). *Why:* the live invariant — you only see who is in the room you are standing in. The "or matched" clause keeps the match reveal working across a refresh and is forward-compatible with Bloc 3 chat.
- **RLS visibility helpers live in a `private` schema, not `public`.** An RLS policy that references the table it protects causes Postgres "infinite recursion in policy", so the visible-id sets are computed in `SECURITY DEFINER` helpers (`my_active_venue_ids`, `visible_profile_ids`) that bypass RLS. They are in a `private` schema PostgREST does not expose, so they are not reachable as `/rest/v1/rpc` endpoints. *Why:* break the recursion safely while keeping the callable API surface minimal (women-first: smallest possible attack surface), even though the data they return is non-sensitive.
- **i18n: code stays English always; user-facing UI text is bilingual FR/EN via a lightweight dictionary, no framework yet.** Code, identifiers, comments, commits remain English (unchanged convention). For the *displayed* strings, we will centralize them in a single dictionary (e.g. `lib/strings.ts`, `{ fr, en }`) and pick the locale from the venue (`venues.city`: Paris → fr, NYC → en), with browser language as the fallback on pre-venue pages (landing, profile). No `next-intl` / locale routing / message catalogs until the surface actually justifies it (plurals, localized dates, many screens, external translation). *Why:* the Paris and NYC test crowds are French- and English-speaking from the very first night, but the whole app is ~15 strings — a full i18n framework would be premature over-engineering; centralizing strings now is the cheap insurance that keeps a later switch to one file, not a hunt across components. Not built yet — planned as a small task at the start of Bloc 1 (see roadmap).
- **Bloc 3 chat is addressed by `match_id`, never by recipient profile id, and expires with the venue night.** The room links to `/chat/[matchId]` only after a reciprocal match exists; messages still reference `match_id`, and RLS requires the sender/reader to be one of the match members. `matches.expires_at` is computed from the venue timezone and the 06:00 local night boundary, and the existing `bartap-close-ended-nights` cron deletes expired matches, cascading messages. *Why:* this keeps "no open DM" structural at both URL and database levels, while making the match/chat disappear with the night so Paramour remains an in-person trigger, not an async inbox.
- **Bloc 4 18+ gate is a mandatory checkbox, stored owner-only in `profile_private`.** We do not collect birthdate and do not use external verification in v1. *Why:* the first real-night test needs a clear adult-only gate without adding sensitive date-of-birth data or a heavy vendor flow before the core spark is proven.
- **Invisible mode is a balanced visibility pause, not silent browsing.** `presence.is_visible=false` keeps the person checked in, hides them from other room grids, and pauses browsing/liking until they become visible again; existing matches/chats remain available. *Why:* women need a fast way to regain control of exposure without leaving the venue, but letting invisible users keep browsing would create an asymmetric, creepy dynamic.
- **Block is strong: hide both ways and close active interactions.** A `blocks` row excludes both profiles from each other's visible sets, prevents new likes/messages, and a trigger deletes existing likes and matches between the pair, cascading messages. *Why:* block is a safety boundary, not a preference filter. Keeping an existing chat alive after a block would undermine the control users expect.
- **Reports are actionable but moderation-light in v1.** A report stores a fixed reason plus an optional note, is readable only by the reporter via RLS, and the UI offers block immediately after reporting. There is no admin console yet. *Why:* v1 needs structured incident data and an immediate self-protection path, while full moderation tooling can wait until there is real usage to moderate.
- **Likes are venue-night scoped, not venue-scoped forever.** Likes carry the same 06:00-local `expires_at` boundary as matches, reciprocal matching requires both likes to share that boundary, and cleanup deletes expired likes with expired matches. *Why:* a like from a previous night should never create tonight's match or keep a person stuck in a stale "Liked" state. This preserves the live, in-person promise: profile persists, but presence, likes, matches, and chats are about tonight.
- **Profile-photo quality has a free baseline now and optional paid AI review later.** The client rejects unsupported/oversized files by default; `PROFILE_PHOTO_REVIEW_ENABLED=true` turns on a server-side vision review before upload, requiring a clear real photo centered on one visible adult human face. *Why:* the required photo is the soft-identity layer that makes a live dating room feel safer, but paid review should not run during routine local development before real venue testing. This is an MVP gate, not final abuse-proofing: the later hardening step is to move Storage upload and profile insert fully server-side so the review cannot be bypassed with direct Supabase calls.

## 2026-06-22

- **Git workflow is issue → branch → PR → squash-merge → delete branch, off `main`.** Branches are `feature/...`/`fix/...` cut from an up-to-date `main`, one branch per PR, reviewed and squash-merged so `main` stays one commit per PR. Issues are optional for trivial work, expected for anything worth tracking. *Why:* it is the GitHub-PR flow we already follow loosely; writing it down makes it the shared default for both founders and any agent, and squash-merge keeps `main` readable. The real failure mode to avoid is letting PRs pile up — a stack of long-lived branches against a moving `main` invites conflicts and DB drift, so open PRs get merged quickly.
- **Schema changes on the single shared remote DB: additive applied freely to test, behavioral/destructive coordinated.** One remote Supabase project is shared by both founders with no local stack, so testing schema-dependent code before a PR means applying the migration to that shared DB. While pre-launch (the shared DB is effectively the dev DB, no real users to protect), an additive migration may be applied early to test; a behavioral or destructive one is announced to the other founder before applying and the PR is merged promptly to resync code and DB. The migration file always ships in the PR. When real users exist, we split a dedicated prod project from this dev one and gate prod migrations to merge time. *Why:* a strict "apply only at merge" rule makes schema-dependent code untestable before the PR on a single shared DB; the additive-vs-behavioral split is what lets us keep testing without breaking the other founder's in-progress `main`. Protecting the shared DB like a prod is premature while there are no users — simplicity first, harden when launch makes it real.
- **Agents do not merge PRs or apply migrations to the shared DB autonomously.** An agent may write code and migration files and open the PR; merging and applying to the remote stay founder-gated. *Why:* both actions are shared and hard to reverse against a resource the other founder depends on, so a human stays in the loop even though agents do the rest of the work.

## 2026-06-28

- **The product name is Paramour.** User-facing app copy and durable product docs use Paramour as the brand name. Existing database object names that include the previous codename remain unchanged unless a migration explicitly renames them. *Why:* the public product needs one consistent name, while already-applied database identifiers should not be rewritten casually.
- **Language is user-selectable across EN/FR/ES.** Venue city and browser language remain the default, but an explicit language choice is stored locally and overrides those defaults across landing, profile, room, and chat. *Why:* test crowds may not match the venue default, and the control should be immediate without account settings or a heavier i18n framework.

## 2026-07-01

- **An admin dashboard is built at `/admin` inside the existing app, not as a separate site.** It is one more route in the same Next.js app / same domain, not a separate deployment or subdomain. *Why:* at this scale a second app is pure maintenance cost for no benefit; a route reuses the existing stack, and access is gated in-app plus by RLS. A subdomain / standalone admin app is a Phase 3 concern (venue dashboard) if ever.
- **Admin access is founder-only via an `admins` allow-list table + RLS, gated by real email/password login — never the service-role in the browser.** A small `public.admins(user_id)` table lists the founders; RLS policies grant admins read access to `reports`/`blocks`/aggregate stats and write access to `venues`. The two founder accounts sign in with Supabase email/password (email provider enabled, accounts created manually via MCP), separate from the public anonymous flow. The service-role key must never reach client code. *Why:* the DB treats every session identically, and an anonymous session cannot see other users' reports — so the DB must recognise the founders as privileged to make the dashboard show anything. An allow-list + RLS stays inside the security model already built (RLS is the real rampart), whereas a server-only service-role dashboard bypasses every RLS guarantee and is dangerous to hand-roll. A custom Postgres role was rejected as over-engineering for two people.
- **The admin auth mechanism is NOT generalised to public users in v1.** Only the two founder accounts get email login; regular visitors stay purely anonymous. *Why:* no premature abstraction — the same Supabase email-auth building block is reused for real user accounts if/when Phase 2 (retention, persistent accounts) needs them, but building a public login now is scope with no v1 payoff.
- **No public user login / profile recovery in v1; optional non-blocking email capture only.** Regular users stay anonymous. Everything valuable (presence, likes, matches, chats) is ephemeral and dies at 06:00; the only persistent thing is the profile (first name + photo + bio), which is cheap to recreate. So cross-device profile *recovery* (a real login flow) is deferred to Phase 2. What may ship in v1 is an *optional, never-blocking* email field framed as "get notified about the next live night", which attaches the email to the same anonymous UUID (and incidentally prepares later recovery). *Why:* the founding principle is zero signup friction at a noisy bar; a login flow solves a 20-second problem (rebuilding a cheap profile) while adding surface and friction. Email capture is a retention/growth hook the roadmap already wants, and is a different thing from login.
- **v1 identity persistence is per-device, and that is accepted.** The anonymous session lives in the browser's localStorage: a returning user on the same phone/browser is recognised automatically (no login), but clearing storage / switching device / private mode orphans the old profile and starts a fresh anonymous user. *Why:* acceptable for v1 because the profile is cheap to recreate and everything else is ephemeral by design. True per-person persistence is what the deferred Phase 2 accounts provide.
- **The v1 landing (`/`) is a gate page with three states, not a login page.** (1) New visitor via typed URL → short pitch + "scan the QR at the bar to join tonight" (no venue is reachable without scanning a QR). (2) Returning visitor with a live session + existing profile → their profile summary + "scan to check in", plus a direct link to any still-active match/chat from tonight. (3) After the night (presence/matches cleaned by the cron) → same as (2), "scan to join tonight". *Why:* in v1 there is no app and no scheduled-soirée concept; the only entry into a venue is scanning the physical QR on the spot. "Reconnecting" is just reopening the app with the stored session, not authenticating.
- **Venues get an `is_live` flag; `check_in()` refuses when a venue is not live.** New `venues.is_live boolean not null default false`. `check_in()` raises if the target venue is not live, closing the "reconnect any afternoon with an old QR link" hole (today `check_in()` checks neither time nor venue state). The admin dashboard is the start/stop switch: *start* sets `is_live=true`; *stop* sets `is_live=false` **and** closes all open presence in that venue (clean room emptying). The 06:00 `close_ended_nights()` cron stays the safety net and additionally flips `is_live=false` for venues whose night has ended, so a forgotten venue self-closes. Dev seed venues (`paris-test`, `nyc-test`) ship `is_live=true` so the dev flow keeps working. *Why:* liveness was purely time+presence with no way to prevent off-hours entry; `is_live` gates *entry* while the cron guarantees *cleanup* — two mechanisms, two distinct roles. This is a **behavioral** migration (it changes `check_in`), so per the shared-DB rule it must be announced to the other founder before applying and the PR merged promptly to resync code and DB.
- **Admin analytics are aggregate-only; no free-browsing of who liked/matched whom.** The dashboard shows per-night/per-venue aggregates (check-ins, likes sent, matches, chats started). "Who liked whom" is never exposed to anyone, including founders (it would break invariant 3 at its root). "Who matched whom" in nominative form is only ever surfaced inside the moderation context of a specific report, never as a browsable list. *Why:* a founder backdoor onto likes/matches destroys the discreet double opt-in trust model the instant it is discovered or leaks; aggregates prove the spark (the Phase 1 goal) without de-anonymising anyone.
- **The admin dashboard's first job is moderation, not analytics.** Reports currently land nowhere (readable only by service-role, no console), so the highest-value surface is a reports/blocks queue the founders can actually see and act on, aligned with the women-first invariant. Venue ops (create + QR + start/stop) is second (it unblocks Bloc 5), aggregate stats third. *Why:* unhandled safety reports are a live gap that directly threatens invariant 5; a nice matches graph is not.
- **Start/stop is a single `SECURITY DEFINER` RPC (`public.set_venue_live`), and `is_live` never moves through a raw `UPDATE`.** Stopping a venue must close *other users'* presence rows, which a founder cannot do under their own RLS, so the operation runs as a definer that self-guards on `private.is_admin()` and does both writes atomically (set `is_live=false` + close all open presence). Venue *creation* stays a normal RLS-gated `INSERT` (`venues_insert_admin`). *Why:* funnelling every liveness change through one guarded path means "stop always empties the room" is structural, and founders never get a broad presence-UPDATE grant.
- **Admin aggregate stats are served only by a counts-only `SECURITY DEFINER` RPC (`public.admin_night_stats`); there is deliberately no admin `SELECT` on `likes`/`matches`.** The function bypasses RLS to count, but returns only per-venue/per-night integers — no ids, no pairs. *Why:* the only way a founder can touch the likes/matches tables is through a function that cannot return anything nominative, making the invariant-3 red line structural rather than a matter of query discipline. (Ephemeral metrics are cleaned nightly, so past nights read 0 for likes/matches/chats — the ephemeral columns are effectively "tonight". Accepted.)
- **The `admins` table is fully locked (RLS on, no policies, no grants); admin-ness is read only through `private.is_admin()`.** Nothing selects `admins` directly; RLS policies and the guarded RPCs call the `SECURITY DEFINER` helper, and the client gate uses a thin `public.am_i_admin()` wrapper. *Why:* the allow-list is a security primitive — the smaller its callable surface, the better. The advisor's `rls_enabled_no_policy` INFO on `admins` is therefore expected, not a gap.
- **The admin UI is English-only internal tooling, outside the FR/EN/ES dictionary.** *Why:* the bilingual dictionary exists for the public, user-facing app; the founder console is internal, and the repo-language rule already mandates English — a second locale set here would be pure overhead.
- **Founder accounts were created directly in `auth.users` via SQL (MCP) with a shared temporary password, not through a signup flow.** The two founder emails were inserted into `auth.users` + `auth.identities` (email provider) and their ids into `public.admins`; the temp password is changed on first use. This account seed is a one-off op, not a migration file. *Why:* the MCP has no admin-create-user tool and there is no public login flow to piggyback on; a direct insert is the pragmatic bootstrap for exactly two internal accounts. Not a pattern for real users — Phase 2 accounts would go through a proper signup/verify flow.
- **The product name changes from Paramour to Amourette (reverses the 2026-06-28 name decision); domain `getamourette.com`.** "Une amourette" = a light, fleeting love affair — it fits the north star (live, discreet, ephemeral, in-person). Due diligence: no dating app of that name on either store, and no software/dating trademark (Class 9/45) — the existing Amourette marks are lingerie (Triumph, Class 25) and perfume (Class 3), different classes. `amourette.com`/`.fr` are taken, hence `getamourette.com`. *Why:* Paramour was blocked/weak; Amourette carries the exact meaning we want. Trade-off accepted: "amourette" is a common French word and an established lingerie brand, so the mark is diluted (weak SEO/ownership) — usable for a dating app but not something we'll ever fully own; a real IP clearance precedes any filing. The Paramour→Amourette rename across UI/docs is a separate chore; DB `bartap-` codename objects stay untouched (per 2026-06-28) unless a migration renames them.
- **Visual identity: dark UI, Bodoni Moda wordmark, a red-derived (monochrome-red) palette.** Dark is the product's ambiance (opened in a bar at night, discreetly), consciously overriding Marwane's Reza-scoped "no dark mode" rule. The logo has three lockups (red / cream / ink) and red is reserved for strong moments; the full palette is derived from a single signature red (`#D01F3C` proposed). Full spec, tokens and open points live in `docs/design.md`; the visual board is `docs/brand/board.html`. *Why:* a dark, editorial, single-red system is coherent with a red wordmark, premium, and ownable in feel even though the *name* is diluted — the DA is where Amourette earns distinctiveness. Three points remain open (red-usage philosophy, exact red, palette confirmation) before tokens are wired into Tailwind.

## 2026-07-02

- **The room becomes a full-screen vertical feed (one profile per viewport), not a card grid.** Scroll-snap feed, big photo for recognition, one like gesture, free scroll back and forth — no swipe deck, no "pass" action, nothing recorded when you scroll past someone. Report/block move behind a `⋯` overflow menu (still ≤ 2 taps away). *Why:* the card grid is catalog language (browse, compare, evaluate) and only exists on desktop, while the product is a phone in a bar; one-person-per-screen matches "these people are here, now", makes a small room feel alive instead of sparse, and avoiding a pass mechanic keeps invariant 3 intact (scrolling past is not a rejection). Burying report/block in a menu keeps them reachable without making every profile read as a threat. Spec: `docs/room-redesign.md`.
- **The pre-crowd wait is a live "room filling up" state, not a static placeholder.** The empty room shows a realtime counter of everyone checked in (not just compatible profiles), copy that sends people back to their night ("check back in a bit"), and a profile-polish CTA. No fake profiles, ever. *Why:* the cold-start minutes are where early arrivals churn; a ticking counter proves the night is real, the honest copy aligns with in-person-first (the app should not demand attention), and profile polish turns dead wait into better matches later. Padding the room with fakes would burn trust and the women-first invariant in one evening.
- **Notifications are a Phase 2 retention chantier, not the v1 cold-start fix; never ask for a PWA install during a night.** When push comes: Android gets the web-push permission prompt at a value moment (never at check-in); iOS requires a home-screen install for push, so iPhone users get honest "check back" copy instead of an install ask mid-party. Install/native-app pitches belong after the night has delivered value (post-match or next-day), like the existing post-like promo modal. *Why:* an install flow at 23:00 with a drink in hand fails; the v1 cold-start answer must ask nothing of the user. Android push is a cheap progressive enhancement later; iOS install is earned retention, not an entry requirement.

## 2026-07-03

- **The art direction is locked: Direction A "Rouge Signature", hybrid red usage (signed by Marwane).** The board v3 confronted the couture-red lead with two fully executed alternatives (B "Heure Bleue" — ink blue + amber glow; C "Dernière Danse" — warm black + neon pink) so the choice was real; A won on the three axes that matter (says "love" immediately, ownable territory — no dating app owns couture red, holds women-first through premium elegance). This closes the three points left open on 2026-07-01: (1) **red usage = hybrid** — punctuation on ~95% of the app, full-red reserved for the match reveal and the splash, so red is an event like the match; (2) **exact red = `#D01F3C`** — settled objectively: of the five candidates only `#D01F3C` and `#C8102E` pass WCAG contrast both as a button fill under cream text (4.5:1) and as a UI element on the velvet background (3:1), and `#D01F3C` is the more couture of the two; (3) **palette confirmed as-is** with two hardened guard-rails — champagne only ever as a 1px hairline (never a fill or button), and blush owns the soft/safety states (invisible mode, safety confirmations, focus ring); "alert red" does not exist in the app, red means love. Full token table (with measured contrasts), type system, component rules and do/don't live in `docs/design.md`; the board is `docs/brand/board.html`. *Why:* the visual system is where a diluted name earns distinctiveness, and every rule above exists to keep the red precious and the app discreet and safe-feeling — the DA translated from the product invariants, not decoration.

## 2026-07-04

- **The Rouge Signature DA is implemented in the app UI: tokens, fonts, and the four public screens.** The nine tokens (velvet, bordeaux, red, wine, champagne, blush, cream, taupe, ink) are wired into Tailwind 4 via `@theme` in `app/globals.css`; fonts are Bodoni Moda (display/wordmark), Inter (body), Jost (uppercase tracked labels/buttons) via `next/font`. The shared `night-*` component classes were rebuilt on the new system (the previous neon/glassmorphism look is gone), then applied to landing, profile, room and chat. Key rule enforcements: the like ♥ is the only red element on the room (taupe outline → solid red, slow fade); safety UI (report/block, error text) is never red — report-submit is a cream-filled affirmative, errors and soft states use blush; scrims fade to velvet, never pure black; the match reveal is a full-red (`#C11B37`) screen, one of the two allowed. `/admin` was left as-is (internal EN-only tooling) beyond inheriting the rebuilt classes. *Why:* executes the DA locked on 2026-07-03; done directly on `main` as an iterate-in-place visual pass (a deliberate one-off exception to the branch/PR workflow, agreed for this setup session).
- **The Amourette wordmark is applied now, ahead of the full copy rename.** The visible wordmark (the big Bodoni title on every screen, the small brand micro-labels, and the browser-tab title) now reads **Amourette**; the rest of the UI copy still says "Paramour" until the separate rename chore lands. *Why:* the DA centres on the wordmark, so shipping it as "Paramour" in Bodoni would have to be redone immediately; flipping just the wordmark is cheap and correct, while the broader Paramour→Amourette copy/identifier sweep stays the separate chore noted on 2026-07-01. Heads-up for Aymane: expect a transient mismatch (wordmark says Amourette, body copy still says Paramour) until that chore is done.
- **Anonymous browser sessions are venue-bound in the client.** Opening a different venue slug from the same browser signs out the current anonymous session and creates a fresh anonymous user for that venue before onboarding/check-in. Profiles remain globally shaped records in the database, and discovery/likes/matches remain scoped by `venue_id`; this client guard prevents local/dev testing from reusing one anonymous identity across NYC and Paris and making rooms look intertwined. *Why:* v1 has no account picker and no physical QR proof yet, so the browser's single Supabase session was too easy to move between test venues. Venue-bound anonymous sessions make the product rule visible now: each bar has its own room.

## 2026-07-09

- **Post-like, the profile stays exactly in place; no unlike in v1.** Liking fills the heart with a soft red bloom (a fade, per the DA motion rules) and nothing else moves: no auto-advance, no reordering, no removal — the feed mirrors the room, and in a bar you keep looking at someone after you've noticed them. A like is final for the night (it expires at 06:00 with everything else). *Why:* auto-advance says "next!" at exactly the moment the user wants to linger; moving liked profiles would make the feed lie about who is in the room. Unlike-before-match is harmless (likes are secret) but rare and adds a delete policy + a race with match creation for little gain. A gentle "close conversation" (unmatch without block) is a separate, women-first workstream noted in the roadmap — today the only exits from a match are block or the 06:00 expiry, which makes politely leaving a conversation heavier than it should be.
- **Tap on a feed profile expands the bio in place; there is no profile detail page.** Bios are clamped to two lines (a long bio must never push the heart off-screen); tapping the card unfolds the full bio over a calmer scrim, tapping again folds it. *Why:* a profile is first name + one photo + bio — a dedicated page would be a door to an empty room. The tap gesture is also the natural slot for multi-photo later.
- **The like vocabulary is written per locale, never translated literally.** FR `Taper/Tapé` read as *hitting someone* and ES `Tocar/Tocado` ("Ambos se tocaron") read as *touching each other* — both were literal translations of the English "tap". New copy: FR **« Craquer / Craqué »** ("Vous avez craqué l'un pour l'autre"), EN keeps **Tap/Tapped** (natural for the discreet gesture), ES **« Flechar / Flechado »** (Cupid's arrow, "Flechazo mutuo") — ES needs a native-speaker pass before any Spanish-market launch. *Why:* the like is the product's core gesture; its word must carry warmth in each language, and a literal translation had already made two of three locales unintentionally comic.
- **The "only revealed if mutual" reminder lives in the one-time first-visit banner only.** The per-card `likeHint` line under every heart was removed. *Why:* the concept is taught once; repeating it under every photo is noise that weighs down the core screen.
- **The room header carries your own avatar (→ profile edit); the language selector moves into the ⋯ room menu.** In-room the header is: wordmark + venue, your avatar, ⋯. Language switching stays directly visible on pre-room surfaces (landing, profile, loading/error shells) where first-time users need it. *Why:* editing your profile had no entry point from the live room at all, while language is a set-once preference that doesn't deserve permanent header space.
- **New arrivals append at the bottom of the feed (arrival-ascending order) and are announced by a cue, never by movement.** The feed order is check-in ascending; a scroll anchor keeps the profile under the thumb in place across list changes (someone above leaving no longer shifts the viewport), and a small "someone just arrived ↓" pill invites the jump. *Why:* the previous newest-first order contradicted its own stability promise — every arrival prepended and shifted the whole feed mid-gesture. The room fills like a real room: from the door, at the end.
- **The room survives a real bar night: foreground resync, realtime recovery, closed-venue screen, heartbeat filtering.** (1) Returning to the foreground or re-subscribing after a websocket drop re-photographs the whole room (candidates, count, matches, unread) instead of trusting missed events — a match that landed while the phone was locked still gets its reveal. (2) Scanning a not-live venue now shows a dedicated "the night hasn't started" screen that reopens itself when a founder flips `is_live` (realtime venues watcher + slow poll fallback), instead of a generic load error; the founder stopping the night drops everyone in the room to the same screen. (3) `presence` gets `REPLICA IDENTITY FULL` (migration `20260709000001`, also adds `venues` to the realtime publication) so clients can skip pure-heartbeat UPDATEs — without it, ~30 phones beating every 2 minutes meant every client reloaded the room every few seconds. The remaining reloads are throttled to one per 2.5 s. *Why:* these were the failure modes we would otherwise have discovered live, in front of real people, on the first real night (Bloc 5).
- **The post-like App-Store promo modal is removed (and the store-URL config with it).** *Why:* it interrupted the product's best moment to point at store pages where no app exists; an install pitch is earned retention for later phases (per the 2026-07-02 notification decision), not a v1 interstitial.
- **Admin stats are venue-first and night-first, with all-nights totals as an explicit investor view.** The dashboard defaults to one venue + one night, and only switches to all nights when a founder asks for cumulative proof. Gender mix is added as aggregate check-in counts (`woman`, `man`, `nonbinary`) inside the counts-only admin stats RPC, not by exposing raw profiles or attendance rows in the browser. *Why:* operating a bar product requires knowing which specific venue/night worked, while investor storytelling needs cumulative traction; both must stay aggregate-only to preserve discreet double opt-in and user safety.
- **QR scan abandonment is tracked as aggregate onboarding analytics, not inferred from auth users.** A `venue_scan_events` row is recorded once per user/venue/night when a venue QR route opens, and the admin stats RPC returns scan counts, profile completions, and drop-offs by venue/night. The browser only inserts through `record_venue_scan`; founders only see aggregate counts. *Why:* "scanned but did not finish profile" is top-of-funnel marketing data that cannot be reconstructed reliably after the fact, and querying raw anonymous auth/profile rows would be both noisy and less privacy-preserving.
- **LGBTQ+ analytics are aggregate preference signals, not identity labels.** The admin dashboard may show same-gender interest, multi-gender interest, and interest in women/men/non-binary people as counts/rates by venue/night, computed from checked-in profiles' `gender` and `interested_in` fields. It does not claim who is LGBTQ+ and never exposes individual identities. *Why:* venue fit and inclusivity matter for growth and safety, but sexuality/identity is sensitive; aggregate product signals are useful, individual labeling is not.
- **Chat-start analytics persist as one aggregate event per match while messages remain ephemeral.** Sending the first message records a `venue_chat_start_events` row keyed by match, venue, and night; the row contains no message content and no profile ids. Admin stats count these rows so conversation-pull survives the nightly deletion of matches/messages. *Why:* privacy cleanup should delete conversations, but founders still need durable aggregate proof that matches turn into conversations.
- **Match analytics persist as one aggregate event while match records remain ephemeral.** `handle_new_like()` still creates a normal expiring `matches` row for the product experience, and also writes a `venue_match_events` row keyed by match, venue, and night. Admin stats count the aggregate event so match efficiency survives cleanup without exposing who matched whom. *Why:* the discreet double opt-in object stays ephemeral for users, while founders retain the venue/night proof needed to understand whether the room is working.
- **Founder ejection is a night-scoped venue safety action, not a permanent account ban.** Admins can eject a reported/blocked profile from a specific venue for the current venue night. The RPC writes an audit row in `venue_ejections`, closes active presence, and `check_in()` refuses re-entry to that venue until the next night. *Why:* v1 moderation needs immediate in-room safety without building a heavy account-ban system; night-scoped ejection fits the live venue model and keeps enforcement proportional.
- **Blocks require a reason and optional note, because they are moderation signals.** Reports already carry structured reasons; blocks now do too, and the admin queue shows that context before a founder ejects someone. *Why:* a block is a user-protection action, but founder enforcement needs context and proportionality. Reasoned blocks keep the queue reviewable without forcing every user to file a full report.

## 2026-07-17

- **Founder profile preview is an explicit test override, not the default room model.** Venue ops can temporarily let checked-in users see completed public profiles when their normal live-room feed is empty; closing the room resets the override. *Why:* founders need to test the scroll feed with sparse attendance, but the product must still default to live, in-room discovery and avoid turning ordinary usage into a general profile browser.
- **Venue Intelligence moves to first-party aggregate analytics with small-cohort privacy suppression.** Existing business tables remain the source of truth for scans, profiles, check-ins, and messages; the new `analytics_events` table records only missing actions (landing/session/venue-open/discovery/profile-view/chat-open) with UTM/QR attribution, while `venue_conversation_events` persists message-count aggregates without message content. Gender/preference insights stay because they are useful for venue fit, but are hidden below a 10-person cohort. *Why:* founders need actionable acquisition, activation, conversation, retention, and venue-quality signals without inventing unsupported features, exposing raw users, or making sensitive claims from tiny samples.

## 2026-07-18

- **Venue Intelligence funnels use a single scanned-user cohort.** The conversion journey counts unique QR scanners, scanned users who created/completed profiles, and scanned users who checked in for the same venue night; raw attendance remains a separate check-in metric. *Why:* mixing total historical presence rows with scan/profile counts made impossible funnels, such as more entrants than profile creators. Founder analytics should compare one cohort through the journey and keep broader attendance as context, not as a conversion denominator.

## 2026-07-22

- **The repo now lives in a GitHub organization (`getamourette`), not a personal account, and is renamed `amourette-webapp`.** The repo was transferred from `AymaneSghier/qr-web-app` to `getamourette/qr-web-app` and then renamed; both founders are org owners; Vercel's GitHub App was granted access to the org and the project reconnected (env vars are on the Vercel project and were preserved; deploys track the repo by id, so the rename was transparent). GitHub keeps redirects from the old paths. *Why:* the whole company's code sat on one founder's personal account (a bus-factor and control risk), and GitHub refuses to link a Project board to a repo owned by a different account — an org fixes both at once. Pre-launch, with no prod users, was the cheapest moment to do it. A GitHub org (not a personal repo) is now the home for all company repos going forward.
- **The GitHub Project `Amourette` is the single shared source of truth for remaining work.** Board at https://github.com/orgs/getamourette/projects/1, linked to the repo, auto-adding new issues to `Inbox`. Fields: `Status` (Inbox → Backlog → Ready → In progress → In review → Done), `Kind`, `Area`, `Owner`, `Priority`. It holds *all* shared work, not just code — bugs, features, design, infra, and non-code ops (marketing/legal/business) via `Area: ops`, since there was previously no shared place for the latter. **Questions live on the board** (`Kind: question`) so they are shared rather than carried in one founder's head; a resolved question becomes a task or a decisions entry. Each founder's personal to-do tool (e.g. Marwane's Todoist) is demoted to private scratch that feeds the board but is never authoritative; `roadmap.md` stays the narrative layer and points at the board. The convention is written up in `AGENTS.md` (Task tracking). *Why:* two founders coding across two timezones need one place that is unambiguously current, or work silently forks; making the board authoritative (and forcing questions and ops onto it) is what stops either founder from carrying the backlog alone.
- **"Clear the other founder's open PRs before starting new work" is an explicit start-of-session ritual.** Reviewing/merging what is ready comes before opening new work, and is now baked into the `standup` skill; `ship` moves a shipped item to `In review` when its PR opens. *Why:* with a ~6h Paris/NY gap, a PR that sits unreviewed while the other founder builds on a moving `main` is exactly how avoidable conflicts and schema drift appear; the review is the guard-rail, not overhead. In the dev phase self-approve stays allowed so neither founder is ever blocked waiting.
- **Self-merge is allowed in the dev phase, except for PRs touching schema/migrations or the product invariants, which need the other founder's eyes before merge even at 0 users.** For ordinary work a founder may open and merge their own PR without waiting (review is encouraged but non-blocking, so nobody is ever stuck across the timezone gap). The exception is any PR that changes the database schema or a migration, or that touches an invariant (discreet double opt-in, no open DM, women-first safety, PII/RLS handling): those wait for the other founder's review before merge. *Why:* self-merge keeps a two-person async team moving, but schema/migration drift and invariant regressions are the two failure modes that are expensive or dangerous to unwind on a shared DB and a product whose whole trust model rests on those red lines, so they are worth the one blocking checkpoint even before there are real users.
- **Board ownership is the native GitHub `Assignee`, not the custom `Owner` field (which is removed).** The per-item owner is now set via GitHub's built-in assignee (`Marwane` = `marwaneazzouzi`, `Aymane` = `AymaneSghier`); the custom single-select `Owner` field was deleted from the board. This supersedes the same-day entry above that listed `Owner` among the fields. `AGENTS.md`, `docs/workflow.md`, and the `/task` and `/standup` skills are updated to read/write `Assignee`. *Why:* the native assignee renders each person's avatar directly on the card (which `Owner` did not), works on both issues and draft items, and gives GitHub-native notifications and "assigned to me" views — so keeping a second custom field alongside it was pure duplication that would drift. Setting it costs a different call than the single-selects (`gh issue edit --add-assignee` for issues, the `updateProjectV2DraftIssue` GraphQL mutation for drafts), documented in the `/task` skill.
- **A fourth skill, `/pick`, owns the "start a task" step, and each task gets its own git worktree.** `/pick` sits between `/standup` and `/ship`: it lists your pickable `Ready` items, converts a chosen draft to a real issue if needed, moves the card to `In progress`, assigns it to you, and cuts the `feature/…`/`fix/…` branch in a dedicated worktree (`<repo-root>--<slug>`), created with `.env.local` copied and deps installed (or reused if it exists). Because a running agent session cannot relocate into another directory, `/pick` ends by printing a ready-to-run launch command (`cd <worktree> && claude "…"` under Claude Code, `codex "…"` under Codex) whose prompt briefs a fresh agent with the task and its issue number, then has it read the issue and **wait for the founder** (to discuss the approach or enter plan mode) rather than build autonomously. `/pick` prepares the environment, it does not start the work. The worktree is created with `git worktree add --no-track` so the branch's upstream is not wrongly set to `origin/main` (which would mislead `/standup`'s drift detection and `git push`); `/ship` sets the real upstream at push time. *Why:* the contract already said "the agent moves `Ready` to `In progress` when it cuts the branch", but no skill executed it, and starting a task by hand (branch, worktree, env, deps, board move) was error-prone. One skill per lifecycle stage (`/task` capture, `/standup` session start, `/pick` task start, `/ship` end) makes the flow uniform across both founders and both agents. Worktree-per-task is what makes parallel agents safe: each task is an isolated checkout, so sessions never fight over the working tree; the only shared-state caveat is the single Supabase DB, so no two parallel tasks should touch the schema at once. Skills depend on `jq` (not installed by default; dropped a static binary into `~/.local/bin`). Two board-read gotchas were fixed across all three board skills at the same time: `gh project item-list` defaults to 30 items (smaller than the active board), so `standup` and `pick` now pass `--limit 500` or active cards fall silently out of view; and `/task` now resolves a freshly created issue's board item id via `gh project item-add --url` (idempotent, returns the id directly) instead of scanning `item-list`, which missed the item to both the 30-item limit and auto-add lag. Auto-archiving `Done` items to keep the active board small is deferred as a separate backlog task.
- **Phone testing standardizes on per-branch Vercel previews, and `DEV_LAN_ORIGIN` / LAN `next dev` testing is removed.** Testing a change on a phone now means opening the branch's Vercel preview at the deterministic URL `https://qr-web-app-git-<branch-slug>-tothe-moon.vercel.app` (the Vercel project is still named `qr-web-app`); `npm run preview:qr` (`scripts/preview-qr.mjs`) builds that URL from the current branch and renders it as a scannable terminal QR via the existing `qrcode` dependency. The `allowedDevOrigins`/`DEV_LAN_ORIGIN` machinery in `next.config.ts`, the `.env.example` entry, and the LAN paragraph in `AGENTS.md` are all removed. *Why:* the LAN path needed each founder's machine IP in `.env.local`, only worked on the same network, served plain HTTP, and could not be shared with the other founder. A Vercel preview is a stable HTTPS URL that exercises the real deployed build and QR flow and is shareable across the Paris/NY gap — better for a two-timezone team. Note for Aymane: this kills LAN `next dev` phone testing for both of us; use `npm run preview:qr` against your pushed branch instead.
- **Design-system rework (#38): the 07-03 "Rouge Signature" DA is being pressure-tested against real screens and is moving; the room feed card (hero #1) is locked on a new direction.** `design.md` turned out to be a one-shot generation, not a considered system, so solidifying it is part of #38. Marwane's call was to treat the 07-03 palette and fonts as **provisional** (dark stays, anchored to the bar-at-night context) and rebuild the visual language screen by screen by rendering comparable variants on the real product surface and choosing (mockups in `docs/brand/explorations/`, served via the existing `python3 -m http.server` LAN pattern). **Hero #1 locked:** the room card = full-bleed chiaroscuro ("Sous les projecteurs"), first name in **Fraunces italic**, cool **ink** ground `#120A0F`, deep signature red `#CC1436` used **present** (filled ♥ at rest), one **champagne 1px hairline** in the footer. Emerging shifts vs 07-03: Bodoni→Fraunces (display/wordmark), Inter→Figtree (body), Jost kept (labels); palette cooler/deeper (velvet→ink, ruby→deep red). The **full token table is rewritten only after the second hero (the match reveal)**, so the spacing/radius/type scales come from two screens, not one. *Why:* deciding on rendered product screens beats debating tokens in the abstract, and the 07-03 doc was never a real system. This **partially supersedes** the 07-03 (Direction A) and 07-04 (Bodoni wordmark + velvet/ruby implementation) entries; those stand as the record of what was considered. **Open point carried to the reveal:** a permanent red ♥ ("red present") softens the 07-03 "red is an event" rule and must be validated so it does not dilute the full-red reveal. The **wordmark/logo is a separate later pass** (reopens the Bodoni wordmark; the unused "wax seal" mock idea is a strong app-icon candidate). **Heads-up for Aymane:** the shipped app UI still shows the 07-04 Bodoni/velvet/ruby look — the new direction is mockups only until the refonte lands, so expect a transient mismatch.

## 2026-07-23

- **A draft PR is shared WIP; only Ready for review means the work is complete and mergeable.** A branch may be pushed without a PR for a Vercel preview/checkpoint, or with a draft PR when shared testing and early feedback are useful; both states remain `In progress`, and draft PRs must not be merged. `/ship` is the explicit final-delivery signal: it runs the full lint+build gate, pushes, creates or updates the PR, marks it Ready for review, verifies that GitHub state, and only then moves the board card to `In review`. Contextual preview/checkpoint requests still push testable WIP with proportionate checks and optionally open/update a draft PR. `/standup` separates ready PRs requiring action from draft PRs shown only as active WIP. Ready is a prerequisite for every merge but does not change the existing self-merge rule or its schema/invariant exceptions. *Why:* an open or draft PR previously meant both "please test this" and "please review/merge this", so founders and agents could not tell whether work was complete; making GitHub's draft/ready state authoritative preserves fast Vercel testing while giving the async Paris/New York workflow one unambiguous review and board signal.

- **Design-system foundation (#38, Étape 0): shadcn/ui adopted on Tailwind 4, v2 tokens and fonts wired, WCAG re-measured.** `shadcn init` (base `radix`, style `radix-nova`) added `components.json`, `lib/utils.ts` (`cn()`), a first `components/ui/button.tsx`, and the deps (`class-variance-authority`, `clsx`, `tailwind-merge`, `radix-ui`, `lucide-react`, `tw-animate-css`); the `shadcn` CLI package itself is a `devDependency` (it is only a build-time `@import "shadcn/tailwind.css"` and the CLI). The v2 palette values from `design.md` are wired into `@theme` in `app/globals.css`, and shadcn's semantic tokens are mapped onto the Amourette palette so shadcn primitives read as Amourette out of the box: `--primary` = `red` / `--primary-foreground` = `cream`; `--card`/`--popover`/`--secondary`/`--muted` = `bordeaux`; `--muted-foreground` = `taupe`; `--accent` = `wine`; `--ring` = `blush` (the focus ring); `--destructive` = `blush` on `ink` (red is love, never danger, so there is no alert red, safety stays blush per the DA); `--border`/`--input` = a faint cream neutral, never champagne, so gold stays precious (one hairline per screen); `--radius: 1.25rem`, which with shadcn's calc multipliers lands exactly on the design radius scale (sm 12, md 16, lg 20, xl 28). Because the app is dark-only (the night is the set), `:root` carries the dark values directly and no `.dark` class is required. Fonts swapped in `app/layout.tsx` via `next/font`: Bodoni to **Fraunces** (display/wordmark/reveal, italic is the brand voice), Inter to **Figtree** (body), Jost kept (labels); `--font-sans` points at Figtree so shadcn primitives inherit the body face (the `@theme inline` self-referential `--font-sans: var(--font-sans)` that `init` writes is a known Tailwind 4 gotcha and was removed). WCAG re-measured for the v2 values: all pass (cream-on-red 4.59:1, red-on-velvet 3.46:1, red-on-bordeaux 3.29:1), closing design open point (1); cream-on-red at 4.59 is the floor, so no text under 12px on a red fill. Validated on a throwaway `design-preview` route (not committed) rendered with the real fonts and compiled tokens. *Why:* the branch (`feature/design-system-shadcn`) commits to shadcn as the component layer, and doing the plumbing (init, token mapping, fonts, contrast) as one non-visual-breaking foundation PR lets the hero rebuilds that follow just consume the system instead of re-deriving it. Mapping shadcn's tokens onto our palette rather than keeping two parallel systems is what keeps standard components (dialogs, inputs, dropdowns) on-brand for free. One gotcha found and fixed: shadcn's generated `button.tsx` imports `Slot` from the unified `radix-ui` barrel (which calls `createContext`) but shipped without a `"use client"` directive, so it 500s in a Next 16 Server Component; the directive was added. The heart's idle/liked classes were updated to v2 values but keep the superseded 07-03 idle-outline behaviour for now; the flip to "red present" lands with the room-card hero rebuild, to keep Étape 0 from changing the existing screens.

## 2026-07-23

- **The v1 email capture is an optional global Amourette-night subscription, not an account upgrade.** After two minutes of active, foreground room use, an unsubscribed user may see a dismissible email modal with a dedicated unchecked marketing-consent box. Dismissing suppresses it for the venue night (the same 06:00-local boundary as presence); a later night may ask again. The owner-only `profile_private` row stores the normalized contact plus consent time/version, while public auth stays anonymous and device-bound. Delivery, verification, and unsubscribe management are separate board tasks (#63 and #64); the unresolved permanent-account choice stays a board question. *Why:* the room must deliver value before asking for a retention channel, marketing consent must stay explicit and separate from authentication, and collecting privately now must not silently expand v1 into login/recovery or weaken the zero-friction QR funnel.
