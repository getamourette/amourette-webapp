# Decisions

Append-only log of architecture and collaboration decisions, shared between both founders. Newest at the bottom. Record the *why*, not just the *what*. Product and strategy decisions live in the Google Doc; this file is for how we build and work.

## 2026-06-19

- **`AGENTS.md` is the single source of truth for agents.** Codex reads `AGENTS.md` natively and `CLAUDE.md` only imports it, so both agents and both founders share one contract instead of two that drift. *Why:* we work with multiple agents (Claude Code, Codex) and a second founder; divergent instructions are how agents end up working blind.
- **Keep Aymane's scaffold as the technical base; rebuild the core mechanic on top.** The scaffold (signup, global directory, open DM) is kept for its working Supabase wiring, but the discreet-like + match-gate mechanic is built over it rather than starting from scratch. *Why:* the wiring is reusable; the open-DM flow violates the discreet double opt-in invariant and cannot ship as is.
- **Phase 1 build order: Bloc 0 (foundations), then 2 (like/match), then 1 (presence), then 3 (chat gate), then 4 (safety).** *Why:* the like/match mechanic is the hypothesis the whole product rests on, so it comes right after the data model it depends on.
- **Repo language is English, conversation can be French.** Code, comments, docs and commits are English; talking to agents in French is fine. *Why:* durable artifacts must be readable by both founders and any future contributor.
- **`docs/` holds the shared roadmap and decision log.** `docs/roadmap.md` for status and plan, `docs/decisions.md` for decisions. *Why:* Todoist is personal to Marwane and Aymane cannot see it; the shared trace must live in the repo.
- **V1 auth: Supabase anonymous sign-in, no account creation at the door.** Scanning the QR triggers `signInAnonymously()` automatically, so every visitor gets a real `auth.users` row (UUID + JWT session) with zero friction — no email, no SMS, no password. *Why:* the product is scan → see who's here, right now; a signup wall at a noisy bar kills the funnel. An anonymous session still gives us `auth.uid()`, so RLS is fully enforceable from day one (security-first) — which "no auth at all" cannot do.
- **Identity model: ephemeral presence and matches, persistent profile.** What persists is the profile (so a regular doesn't rebuild it every night). What is ephemeral is presence (fades on leave/timeout) **and the match/chat** (dies with the night). *Why:* the ephemerality of the match is the forcing function — you matched, you have *tonight* to go talk IRL. Persisting matches would reintroduce the async pen-pal dynamic the north star rejects (in-person first). We never promise users they can "retrieve their matches later".
- **Profile = first name + photo (required) + bio (optional). Email is optional and collected later, never at entry.** The required photo + first name is the soft identity / women-first layer (you see a real face), not a verified email. Email is offered after onboarding, framed as "get notified about the next live night" — it triggers the anonymous→permanent account upgrade (same UUID, profile becomes cross-device recoverable). *Why:* email earns us a re-engagement channel and the only real returning-user retention metric, but framed around bringing people back to a *live* room, not maintaining async chats. Email is PII: it must never be exposed to other users (RLS, no `select("*")`), and storing it touches the open RGPD / Supabase-region (EU vs US) question to settle with Aymane before any public test.
- **PII lives in a separate `profile_private` table, not in `profiles`.** `profiles` holds only public columns (first name, photo, bio, gender, interested_in); email/phone go in `profile_private` with owner-only RLS on every operation. *Why:* RLS is row-level and cannot hide a column of a row that is otherwise readable. Isolating PII in its own table is the only hard guarantee email never leaks — it does not depend on every future query remembering to avoid `select("*")`.
- **Discreet like enforced in RLS, not just the UI.** `likes` SELECT is `liker_id = auth.uid()`: you can read only the likes you sent, never one targeting you. The only signal that surfaces is a `matches` row, and only when mutual. *Why:* invariant 3 (no one is ever told they were liked) must hold at the database, so a bug or a direct API call can't leak "who liked me".
- **Matches are created by a `SECURITY DEFINER` trigger on `likes`, never by users.** `handle_new_like()` checks for the reciprocal like in the same venue and inserts the ordered pair into `matches`; EXECUTE is revoked from the API roles. *Why:* the reciprocity check must read `likes` rows the user is not allowed to SELECT, so it runs as definer; keeping inserts out of users' hands means the match is the single trustworthy mutual signal.
- **Messages are tied to a `match_id` (no open DM possible).** A message references a match and RLS requires the sender to be one of its two members. *Why:* makes the "no open messaging" red line structural — a message literally cannot exist without a prior mutual match.
- **Profile carries `gender` + `interested_in` (set of woman/man/nonbinary) from the start.** *Why:* `interested_in` is meaningless without the other person's `gender` to filter on, so both ship together rather than bolting gender on later. (`interested_in` is an array to allow more than one.)
- **Likes and matches are venue-scoped; ephemerality (expiry/cleanup) is deferred to Bloc 1/3.** Foundations ship the tables with no TTL yet; the "match dies with the night" cleanup job is a later bloc (noted in roadmap so it isn't forgotten). *Why:* keep Bloc 0 to the data model; the forcing-function expiry needs the presence timeout logic that lands with Bloc 1.
- **`NEXT_PUBLIC_SUPABASE_ANON_KEY` renamed to `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`.** The client now uses the new `sb_publishable_...` key, and `lib/supabase.ts` is typed with the generated `Database` type (`lib/database.types.ts`). *Why:* Supabase's new publishable key is the recommended browser key; the var name now matches reality.
- **Presence model: "the room of tonight", not a minute-by-minute timeout.** You are present from check-in until you explicitly leave or the night rolls over at a fixed local hour. There is **no short heartbeat timeout that hides you**. *Why:* a heartbeat only fires while the tab is open and active; at a bar the phone is in a pocket, screen locked. A short timeout would drop people who are physically in the room, and we cannot distinguish "phone in pocket, still here" from "went home" — both just stop heart-beating. A short window also empties the room and kills the cold-start. The "live" invariant is honoured at the granularity of *tonight*, which is the right grain for a bar. Risk accepted: you might match with someone who already left — but the match is ephemeral and the promise is "they're here tonight", same as real life.
- **The night ends at 06:00 local time, enforced by a `pg_cron` job (`bartap-close-ended-nights`, every 15 min).** `close_ended_nights()` sets `left_at` on every active presence past the first 06:00 (in the venue's timezone) strictly after its check-in — handles after-midnight check-ins (02:00 belongs to the night that ends that same morning). Venues carry an explicit `timezone` column (Paris → `Europe/Paris`, NYC → `America/New_York`) so the rollover does not hardcode a city map. *Why:* this cron, not a client timeout, is what empties the room for good; 06:00 maps to a bar closing and is predictable.
- **`check_in(venue)` RPC is the single check-in path: one room at a time, idempotent, also the heartbeat.** It leaves any active presence in a *different* venue, then bumps `last_seen_at` if already here or inserts a fresh row. `SECURITY INVOKER` so it runs under the caller's own RLS. *Why:* a clean atomic upsert against the partial unique index (`one active presence per profile`) without client-side juggling; reusing it as the heartbeat keeps one code path.
- **Explicit "leave the room" control ships in Bloc 1, not deferred to Bloc 4.** A button sets `left_at` and removes you from the room immediately. *Why:* it is cheap, it is the natural complement to the night-session model (the only way to disappear before the rollover), and it is a first concrete women-first control over your own visibility.
- **`profiles` and `presence` SELECT tightened to co-present users now that presence exists.** `profiles` is readable only for yourself, anyone co-present in a venue you are currently in, or anyone you have matched with (so a match stays readable after they leave); `presence` only for your own rows and the venues you are in. This closes the intentional Bloc 0 hole (profiles readable by any authenticated session). *Why:* the live invariant — you only see who is in the room you are standing in. The "or matched" clause keeps the match reveal working across a refresh and is forward-compatible with Bloc 3 chat.
- **RLS visibility helpers live in a `private` schema, not `public`.** An RLS policy that references the table it protects causes Postgres "infinite recursion in policy", so the visible-id sets are computed in `SECURITY DEFINER` helpers (`my_active_venue_ids`, `visible_profile_ids`) that bypass RLS. They are in a `private` schema PostgREST does not expose, so they are not reachable as `/rest/v1/rpc` endpoints. *Why:* break the recursion safely while keeping the callable API surface minimal (women-first: smallest possible attack surface), even though the data they return is non-sensitive.
- **i18n: code stays English always; user-facing UI text is bilingual FR/EN via a lightweight dictionary, no framework yet.** Code, identifiers, comments, commits remain English (unchanged convention). For the *displayed* strings, we will centralize them in a single dictionary (e.g. `lib/strings.ts`, `{ fr, en }`) and pick the locale from the venue (`venues.city`: Paris → fr, NYC → en), with browser language as the fallback on pre-venue pages (landing, profile). No `next-intl` / locale routing / message catalogs until the surface actually justifies it (plurals, localized dates, many screens, external translation). *Why:* the Paris and NYC test crowds are French- and English-speaking from the very first night, but the whole app is ~15 strings — a full i18n framework would be premature over-engineering; centralizing strings now is the cheap insurance that keeps a later switch to one file, not a hunt across components. Not built yet — planned as a small task at the start of Bloc 1 (see roadmap).
- **Bloc 3 chat is addressed by `match_id`, never by recipient profile id, and expires with the venue night.** The room links to `/chat/[matchId]` only after a reciprocal match exists; messages still reference `match_id`, and RLS requires the sender/reader to be one of the match members. `matches.expires_at` is computed from the venue timezone and the 06:00 local night boundary, and the existing `bartap-close-ended-nights` cron deletes expired matches, cascading messages. *Why:* this keeps "no open DM" structural at both URL and database levels, while making the match/chat disappear with the night so Paramour remains an in-person trigger, not an async inbox.
- **Bloc 4 18+ gate is a mandatory checkbox, stored owner-only in `profile_private`.** We do not collect birthdate and do not use external verification in v1. *Why:* the first real-night test needs a clear adult-only gate without adding sensitive date-of-birth data or a heavy vendor flow before the core spark is proven.
- **Invisible mode is a balanced visibility pause, not silent browsing.** `presence.is_visible=false` keeps the person checked in, hides them from other room grids, and pauses browsing/liking until they become visible again; existing matches/chats remain available. *Why:* women need a fast way to regain control of exposure without leaving the venue, but letting invisible users keep browsing would create an asymmetric, creepy dynamic.
- **Block is strong: hide both ways and close active interactions.** A `blocks` row excludes both profiles from each other's visible sets, prevents new likes/messages, and a trigger deletes existing likes and matches between the pair, cascading messages. *Why:* block is a safety boundary, not a preference filter. Keeping an existing chat alive after a block would undermine the control users expect.
- **Reports are actionable but moderation-light in v1.** A report stores a fixed reason plus an optional note, is readable only by the reporter via RLS, and the UI offers block immediately after reporting. There is no admin console yet. *Why:* v1 needs structured incident data and an immediate self-protection path, while full moderation tooling can wait until there is real usage to moderate.
- **Likes are venue-night scoped, not venue-scoped forever.** Likes carry the same 06:00-local `expires_at` boundary as matches, reciprocal matching requires both likes to share that boundary, and cleanup deletes expired likes with expired matches. *Why:* a like from a previous night should never create tonight's match or keep a person stuck in a stale "Liked" state. This preserves the live, in-person promise: profile persists, but presence, likes, matches, and chats are about tonight.
- **Profile-photo quality has a free baseline now and optional paid AI review later.** The client rejects unsupported/oversized files by default; `PROFILE_PHOTO_REVIEW_ENABLED=true` turns on a server-side vision review before upload, requiring a clear real photo centered on one visible adult human face. *Why:* the required photo is the soft-identity layer that makes a live dating room feel safer, but paid review should not run during routine local development before real venue testing. This is an MVP gate, not final abuse-proofing: the later hardening step is to move Storage upload and profile insert fully server-side so the review cannot be bypassed with direct Supabase calls.

## 2026-06-22

- **Git workflow is issue → branch → PR → squash-merge → delete branch, off `main`.** Branches are `feature/...`/`fix/...` cut from an up-to-date `main`, one branch per PR, reviewed and squash-merged so `main` stays one commit per PR. Issues are optional for trivial work, expected for anything worth tracking. *Why:* it is the GitHub-PR flow we already follow loosely; writing it down makes it the shared default for both founders and any agent, and squash-merge keeps `main` readable. The real failure mode to avoid is letting PRs pile up — a stack of long-lived branches against a moving `main` invites conflicts and DB drift, so open PRs get merged quickly.
- **Schema changes on the single shared remote DB: additive applied freely to test, behavioral/destructive coordinated.** One remote Supabase project is shared by both founders with no local stack, so testing schema-dependent code before a PR means applying the migration to that shared DB. While pre-launch (the shared DB is effectively the dev DB, no real users to protect), an additive migration may be applied early to test; a behavioral or destructive one is announced to the other founder before applying and the PR is merged promptly to resync code and DB. The migration file always ships in the PR. When real users exist, we split a dedicated prod project from this dev one and gate prod migrations to merge time. *Why:* a strict "apply only at merge" rule makes schema-dependent code untestable before the PR on a single shared DB; the additive-vs-behavioral split is what lets us keep testing without breaking the other founder's in-progress `main`. Protecting the shared DB like a prod is premature while there are no users — simplicity first, harden when launch makes it real.
- **Agents do not merge PRs or apply migrations to the shared DB autonomously.** An agent may write code and migration files and open the PR; merging and applying to the remote stay founder-gated. *Why:* both actions are shared and hard to reverse against a resource the other founder depends on, so a human stays in the loop even though agents do the rest of the work.

## 2026-06-28

- **The product name is Paramour.** User-facing app copy and durable product docs use Paramour as the brand name. Existing database object names that include the previous codename remain unchanged unless a migration explicitly renames them. *Why:* the public product needs one consistent name, while already-applied database identifiers should not be rewritten casually.
- **Language is user-selectable across EN/FR/ES.** Venue city and browser language remain the default, but an explicit language choice is stored locally and overrides those defaults across landing, profile, room, and chat. *Why:* test crowds may not match the venue default, and the control should be immediate without account settings or a heavier i18n framework.

## 2026-07-01

- **An admin dashboard is built at `/admin` inside the existing app, not as a separate site.** It is one more route in the same Next.js app / same domain, not a separate deployment or subdomain. *Why:* at this scale a second app is pure maintenance cost for no benefit; a route reuses the existing stack, and access is gated in-app plus by RLS. A subdomain / standalone admin app is a Phase 3 concern (venue dashboard) if ever.
- **Admin access is founder-only via an `admins` allow-list table + RLS, gated by real email/password login — never the service-role in the browser.** A small `public.admins(user_id)` table lists the founders; RLS policies grant admins read access to `reports`/`blocks`/aggregate stats and write access to `venues`. The two founder accounts sign in with Supabase email/password (email provider enabled, accounts created manually via MCP), separate from the public anonymous flow. The service-role key must never reach client code. *Why:* the DB treats every session identically, and an anonymous session cannot see other users' reports — so the DB must recognise the founders as privileged to make the dashboard show anything. An allow-list + RLS stays inside the security model already built (RLS is the real rampart), whereas a server-only service-role dashboard bypasses every RLS guarantee and is dangerous to hand-roll. A custom Postgres role was rejected as over-engineering for two people.
- **The admin auth mechanism is NOT generalised to public users in v1.** Only the two founder accounts get email login; regular visitors stay purely anonymous. *Why:* no premature abstraction — the same Supabase email-auth building block is reused for real user accounts if/when Phase 2 (retention, persistent accounts) needs them, but building a public login now is scope with no v1 payoff.
- **No public user login / profile recovery in v1; optional non-blocking email capture only.** Regular users stay anonymous. Everything valuable (presence, likes, matches, chats) is ephemeral and dies at 06:00; the only persistent thing is the profile (first name + photo + bio), which is cheap to recreate. So cross-device profile *recovery* (a real login flow) is deferred to Phase 2. What may ship in v1 is an *optional, never-blocking* email field framed as "get notified about the next live night", which attaches the email to the same anonymous UUID (and incidentally prepares later recovery). *Why:* the founding principle is zero signup friction at a noisy bar; a login flow solves a 20-second problem (rebuilding a cheap profile) while adding surface and friction. Email capture is a retention/growth hook the roadmap already wants, and is a different thing from login.
- **v1 identity persistence is per-device, and that is accepted.** The anonymous session lives in the browser's localStorage: a returning user on the same phone/browser is recognised automatically (no login), but clearing storage / switching device / private mode orphans the old profile and starts a fresh anonymous user. *Why:* acceptable for v1 because the profile is cheap to recreate and everything else is ephemeral by design. True per-person persistence is what the deferred Phase 2 accounts provide.
- **The v1 landing (`/`) is a gate page with three states, not a login page.** (1) New visitor via typed URL → short pitch + "scan the QR at the bar to join tonight" (no venue is reachable without scanning a QR). (2) Returning visitor with a live session + existing profile → their profile summary + "scan to check in", plus a direct link to any still-active match/chat from tonight. (3) After the night (presence/matches cleaned by the cron) → same as (2), "scan to join tonight". *Why:* in v1 there is no app and no scheduled-soirée concept; the only entry into a venue is scanning the physical QR on the spot. "Reconnecting" is just reopening the app with the stored session, not authenticating.
- **Venues get an `is_live` flag; `check_in()` refuses when a venue is not live.** New `venues.is_live boolean not null default false`. `check_in()` raises if the target venue is not live, closing the "reconnect any afternoon with an old QR link" hole (today `check_in()` checks neither time nor venue state). The admin dashboard is the start/stop switch: *start* sets `is_live=true`; *stop* sets `is_live=false` **and** closes all open presence in that venue (clean room emptying). The 06:00 `close_ended_nights()` cron stays the safety net and additionally flips `is_live=false` for venues whose night has ended, so a forgotten venue self-closes. Dev seed venues (`paris-test`, `nyc-test`) ship `is_live=true` so the dev flow keeps working. *Why:* liveness was purely time+presence with no way to prevent off-hours entry; `is_live` gates *entry* while the cron guarantees *cleanup* — two mechanisms, two distinct roles. This is a **behavioral** migration (it changes `check_in`), so per the shared-DB rule it must be announced to the other founder before applying and the PR merged promptly to resync code and DB.
- **Admin analytics are aggregate-only; no free-browsing of who liked/matched whom.** The dashboard shows per-night/per-venue aggregates (check-ins, likes sent, matches, chats started). "Who liked whom" is never exposed to anyone, including founders (it would break invariant 3 at its root). "Who matched whom" in nominative form is only ever surfaced inside the moderation context of a specific report, never as a browsable list. *Why:* a founder backdoor onto likes/matches destroys the discreet double opt-in trust model the instant it is discovered or leaks; aggregates prove the spark (the Phase 1 goal) without de-anonymising anyone.
- **The admin dashboard's first job is moderation, not analytics.** Reports currently land nowhere (readable only by service-role, no console), so the highest-value surface is a reports/blocks queue the founders can actually see and act on, aligned with the women-first invariant. Venue ops (create + QR + start/stop) is second (it unblocks Bloc 5), aggregate stats third. *Why:* unhandled safety reports are a live gap that directly threatens invariant 5; a nice matches graph is not.
- **Start/stop is a single `SECURITY DEFINER` RPC (`public.set_venue_live`), and `is_live` never moves through a raw `UPDATE`.** Stopping a venue must close *other users'* presence rows, which a founder cannot do under their own RLS, so the operation runs as a definer that self-guards on `private.is_admin()` and does both writes atomically (set `is_live=false` + close all open presence). Venue *creation* stays a normal RLS-gated `INSERT` (`venues_insert_admin`). *Why:* funnelling every liveness change through one guarded path means "stop always empties the room" is structural, and founders never get a broad presence-UPDATE grant.
- **Admin aggregate stats are served only by a counts-only `SECURITY DEFINER` RPC (`public.admin_night_stats`); there is deliberately no admin `SELECT` on `likes`/`matches`.** The function bypasses RLS to count, but returns only per-venue/per-night integers — no ids, no pairs. *Why:* the only way a founder can touch the likes/matches tables is through a function that cannot return anything nominative, making the invariant-3 red line structural rather than a matter of query discipline. (Ephemeral metrics are cleaned nightly, so past nights read 0 for likes/matches/chats — the ephemeral columns are effectively "tonight". Accepted.)
- **The `admins` table is fully locked (RLS on, no policies, no grants); admin-ness is read only through `private.is_admin()`.** Nothing selects `admins` directly; RLS policies and the guarded RPCs call the `SECURITY DEFINER` helper, and the client gate uses a thin `public.am_i_admin()` wrapper. *Why:* the allow-list is a security primitive — the smaller its callable surface, the better. The advisor's `rls_enabled_no_policy` INFO on `admins` is therefore expected, not a gap.
- **The admin UI is English-only internal tooling, outside the FR/EN/ES dictionary.** *Why:* the bilingual dictionary exists for the public, user-facing app; the founder console is internal, and the repo-language rule already mandates English — a second locale set here would be pure overhead.
- **Founder accounts were created directly in `auth.users` via SQL (MCP) with a shared temporary password, not through a signup flow.** The two founder emails were inserted into `auth.users` + `auth.identities` (email provider) and their ids into `public.admins`; the temp password is changed on first use. This account seed is a one-off op, not a migration file. *Why:* the MCP has no admin-create-user tool and there is no public login flow to piggyback on; a direct insert is the pragmatic bootstrap for exactly two internal accounts. Not a pattern for real users — Phase 2 accounts would go through a proper signup/verify flow.
- **The product name changes from Paramour to Amourette (reverses the 2026-06-28 name decision); domain `getamourette.com`.** "Une amourette" = a light, fleeting love affair — it fits the north star (live, discreet, ephemeral, in-person). Due diligence: no dating app of that name on either store, and no software/dating trademark (Class 9/45) — the existing Amourette marks are lingerie (Triumph, Class 25) and perfume (Class 3), different classes. `amourette.com`/`.fr` are taken, hence `getamourette.com`. *Why:* Paramour was blocked/weak; Amourette carries the exact meaning we want. Trade-off accepted: "amourette" is a common French word and an established lingerie brand, so the mark is diluted (weak SEO/ownership) — usable for a dating app but not something we'll ever fully own; a real IP clearance precedes any filing. The Paramour→Amourette rename across UI/docs is a separate chore; DB `bartap-` codename objects stay untouched (per 2026-06-28) unless a migration renames them.
- **Visual identity: dark UI, Bodoni Moda wordmark, a red-derived (monochrome-red) palette.** Dark is the product's ambiance (opened in a bar at night, discreetly), consciously overriding Marwane's Reza-scoped "no dark mode" rule. The logo has three lockups (red / cream / ink) and red is reserved for strong moments; the full palette is derived from a single signature red (`#D01F3C` proposed). Full spec, tokens and open points live in `docs/design.md`; the visual board is `docs/brand/board.html`. *Why:* a dark, editorial, single-red system is coherent with a red wordmark, premium, and ownable in feel even though the *name* is diluted — the DA is where Amourette earns distinctiveness. Three points remain open (red-usage philosophy, exact red, palette confirmation) before tokens are wired into Tailwind.

## 2026-07-02

- **The room becomes a full-screen vertical feed (one profile per viewport), not a card grid.** Scroll-snap feed, big photo for recognition, one like gesture, free scroll back and forth — no swipe deck, no "pass" action, nothing recorded when you scroll past someone. Report/block move behind a `⋯` overflow menu (still ≤ 2 taps away). *Why:* the card grid is catalog language (browse, compare, evaluate) and only exists on desktop, while the product is a phone in a bar; one-person-per-screen matches "these people are here, now", makes a small room feel alive instead of sparse, and avoiding a pass mechanic keeps invariant 3 intact (scrolling past is not a rejection). Burying report/block in a menu keeps them reachable without making every profile read as a threat. Spec: `docs/room-redesign.md`.
- **The pre-crowd wait is a live "room filling up" state, not a static placeholder.** The empty room shows a realtime counter of everyone checked in (not just compatible profiles), copy that sends people back to their night ("check back in a bit"), and a profile-polish CTA. No fake profiles, ever. *Why:* the cold-start minutes are where early arrivals churn; a ticking counter proves the night is real, the honest copy aligns with in-person-first (the app should not demand attention), and profile polish turns dead wait into better matches later. Padding the room with fakes would burn trust and the women-first invariant in one evening.
- **Notifications are a Phase 2 retention chantier, not the v1 cold-start fix; never ask for a PWA install during a night.** When push comes: Android gets the web-push permission prompt at a value moment (never at check-in); iOS requires a home-screen install for push, so iPhone users get honest "check back" copy instead of an install ask mid-party. Install/native-app pitches belong after the night has delivered value (post-match or next-day), like the existing post-like promo modal. *Why:* an install flow at 23:00 with a drink in hand fails; the v1 cold-start answer must ask nothing of the user. Android push is a cheap progressive enhancement later; iOS install is earned retention, not an entry requirement.

## 2026-07-03

- **The art direction is locked: Direction A "Rouge Signature", hybrid red usage (signed by Marwane).** The board v3 confronted the couture-red lead with two fully executed alternatives (B "Heure Bleue" — ink blue + amber glow; C "Dernière Danse" — warm black + neon pink) so the choice was real; A won on the three axes that matter (says "love" immediately, ownable territory — no dating app owns couture red, holds women-first through premium elegance). This closes the three points left open on 2026-07-01: (1) **red usage = hybrid** — punctuation on ~95% of the app, full-red reserved for the match reveal and the splash, so red is an event like the match; (2) **exact red = `#D01F3C`** — settled objectively: of the five candidates only `#D01F3C` and `#C8102E` pass WCAG contrast both as a button fill under cream text (4.5:1) and as a UI element on the velvet background (3:1), and `#D01F3C` is the more couture of the two; (3) **palette confirmed as-is** with two hardened guard-rails — champagne only ever as a 1px hairline (never a fill or button), and blush owns the soft/safety states (invisible mode, safety confirmations, focus ring); "alert red" does not exist in the app, red means love. Full token table (with measured contrasts), type system, component rules and do/don't live in `docs/design.md`; the board is `docs/brand/board.html`. *Why:* the visual system is where a diluted name earns distinctiveness, and every rule above exists to keep the red precious and the app discreet and safe-feeling — the DA translated from the product invariants, not decoration.

## 2026-07-04

- **The Rouge Signature DA is implemented in the app UI: tokens, fonts, and the four public screens.** The nine tokens (velvet, bordeaux, red, wine, champagne, blush, cream, taupe, ink) are wired into Tailwind 4 via `@theme` in `app/globals.css`; fonts are Bodoni Moda (display/wordmark), Inter (body), Jost (uppercase tracked labels/buttons) via `next/font`. The shared `night-*` component classes were rebuilt on the new system (the previous neon/glassmorphism look is gone), then applied to landing, profile, room and chat. Key rule enforcements: the like ♥ is the only red element on the room (taupe outline → solid red, slow fade); safety UI (report/block, error text) is never red — report-submit is a cream-filled affirmative, errors and soft states use blush; scrims fade to velvet, never pure black; the match reveal is a full-red (`#C11B37`) screen, one of the two allowed. `/admin` was left as-is (internal EN-only tooling) beyond inheriting the rebuilt classes. *Why:* executes the DA locked on 2026-07-03; done directly on `main` as an iterate-in-place visual pass (a deliberate one-off exception to the branch/PR workflow, agreed for this setup session).
- **The Amourette wordmark is applied now, ahead of the full copy rename.** The visible wordmark (the big Bodoni title on every screen, the small brand micro-labels, and the browser-tab title) now reads **Amourette**; the rest of the UI copy still says "Paramour" until the separate rename chore lands. *Why:* the DA centres on the wordmark, so shipping it as "Paramour" in Bodoni would have to be redone immediately; flipping just the wordmark is cheap and correct, while the broader Paramour→Amourette copy/identifier sweep stays the separate chore noted on 2026-07-01. Heads-up for Aymane: expect a transient mismatch (wordmark says Amourette, body copy still says Paramour) until that chore is done.
- **Anonymous browser sessions are venue-bound in the client.** Opening a different venue slug from the same browser signs out the current anonymous session and creates a fresh anonymous user for that venue before onboarding/check-in. Profiles remain globally shaped records in the database, and discovery/likes/matches remain scoped by `venue_id`; this client guard prevents local/dev testing from reusing one anonymous identity across NYC and Paris and making rooms look intertwined. *Why:* v1 has no account picker and no physical QR proof yet, so the browser's single Supabase session was too easy to move between test venues. Venue-bound anonymous sessions make the product rule visible now: each bar has its own room.

## 2026-07-09

- **Post-like, the profile stays exactly in place; no unlike in v1.** Liking fills the heart with a soft red bloom (a fade, per the DA motion rules) and nothing else moves: no auto-advance, no reordering, no removal — the feed mirrors the room, and in a bar you keep looking at someone after you've noticed them. A like is final for the night (it expires at 06:00 with everything else). *Why:* auto-advance says "next!" at exactly the moment the user wants to linger; moving liked profiles would make the feed lie about who is in the room. Unlike-before-match is harmless (likes are secret) but rare and adds a delete policy + a race with match creation for little gain. A gentle "close conversation" (unmatch without block) is a separate, women-first workstream noted in the roadmap — today the only exits from a match are block or the 06:00 expiry, which makes politely leaving a conversation heavier than it should be.
- **Tap on a feed profile expands the bio in place; there is no profile detail page.** Bios are clamped to two lines (a long bio must never push the heart off-screen); tapping the card unfolds the full bio over a calmer scrim, tapping again folds it. *Why:* a profile is first name + one photo + bio — a dedicated page would be a door to an empty room. The tap gesture is also the natural slot for multi-photo later.
- **The like vocabulary is written per locale, never translated literally.** FR `Taper/Tapé` read as *hitting someone* and ES `Tocar/Tocado` ("Ambos se tocaron") read as *touching each other* — both were literal translations of the English "tap". New copy: FR **« Craquer / Craqué »** ("Vous avez craqué l'un pour l'autre"), EN keeps **Tap/Tapped** (natural for the discreet gesture), ES **« Flechar / Flechado »** (Cupid's arrow, "Flechazo mutuo") — ES needs a native-speaker pass before any Spanish-market launch. *Why:* the like is the product's core gesture; its word must carry warmth in each language, and a literal translation had already made two of three locales unintentionally comic.
- **The "only revealed if mutual" reminder lives in the one-time first-visit banner only.** The per-card `likeHint` line under every heart was removed. *Why:* the concept is taught once; repeating it under every photo is noise that weighs down the core screen.
- **The room header carries your own avatar (→ profile edit); the language selector moves into the ⋯ room menu.** In-room the header is: wordmark + venue, your avatar, ⋯. Language switching stays directly visible on pre-room surfaces (landing, profile, loading/error shells) where first-time users need it. *Why:* editing your profile had no entry point from the live room at all, while language is a set-once preference that doesn't deserve permanent header space.
- **New arrivals append at the bottom of the feed (arrival-ascending order) and are announced by a cue, never by movement.** The feed order is check-in ascending; a scroll anchor keeps the profile under the thumb in place across list changes (someone above leaving no longer shifts the viewport), and a small "someone just arrived ↓" pill invites the jump. *Why:* the previous newest-first order contradicted its own stability promise — every arrival prepended and shifted the whole feed mid-gesture. The room fills like a real room: from the door, at the end.
- **The room survives a real bar night: foreground resync, realtime recovery, closed-venue screen, heartbeat filtering.** (1) Returning to the foreground or re-subscribing after a websocket drop re-photographs the whole room (candidates, count, matches, unread) instead of trusting missed events — a match that landed while the phone was locked still gets its reveal. (2) Scanning a not-live venue now shows a dedicated "the night hasn't started" screen that reopens itself when a founder flips `is_live` (realtime venues watcher + slow poll fallback), instead of a generic load error; the founder stopping the night drops everyone in the room to the same screen. (3) `presence` gets `REPLICA IDENTITY FULL` (migration `20260709000001`, also adds `venues` to the realtime publication) so clients can skip pure-heartbeat UPDATEs — without it, ~30 phones beating every 2 minutes meant every client reloaded the room every few seconds. The remaining reloads are throttled to one per 2.5 s. *Why:* these were the failure modes we would otherwise have discovered live, in front of real people, on the first real night (Bloc 5).
- **The post-like App-Store promo modal is removed (and the store-URL config with it).** *Why:* it interrupted the product's best moment to point at store pages where no app exists; an install pitch is earned retention for later phases (per the 2026-07-02 notification decision), not a v1 interstitial.
- **Admin stats are venue-first and night-first, with all-nights totals as an explicit investor view.** The dashboard defaults to one venue + one night, and only switches to all nights when a founder asks for cumulative proof. Gender mix is added as aggregate check-in counts (`woman`, `man`, `nonbinary`) inside the counts-only admin stats RPC, not by exposing raw profiles or attendance rows in the browser. *Why:* operating a bar product requires knowing which specific venue/night worked, while investor storytelling needs cumulative traction; both must stay aggregate-only to preserve discreet double opt-in and user safety.
- **QR scan abandonment is tracked as aggregate onboarding analytics, not inferred from auth users.** A `venue_scan_events` row is recorded once per user/venue/night when a venue QR route opens, and the admin stats RPC returns scan counts, profile completions, and drop-offs by venue/night. The browser only inserts through `record_venue_scan`; founders only see aggregate counts. *Why:* "scanned but did not finish profile" is top-of-funnel marketing data that cannot be reconstructed reliably after the fact, and querying raw anonymous auth/profile rows would be both noisy and less privacy-preserving.
- **LGBTQ+ analytics are aggregate preference signals, not identity labels.** The admin dashboard may show same-gender interest, multi-gender interest, and interest in women/men/non-binary people as counts/rates by venue/night, computed from checked-in profiles' `gender` and `interested_in` fields. It does not claim who is LGBTQ+ and never exposes individual identities. *Why:* venue fit and inclusivity matter for growth and safety, but sexuality/identity is sensitive; aggregate product signals are useful, individual labeling is not.
- **Chat-start analytics persist as one aggregate event per match while messages remain ephemeral.** Sending the first message records a `venue_chat_start_events` row keyed by match, venue, and night; the row contains no message content and no profile ids. Admin stats count these rows so conversation-pull survives the nightly deletion of matches/messages. *Why:* privacy cleanup should delete conversations, but founders still need durable aggregate proof that matches turn into conversations.
- **Match analytics persist as one aggregate event while match records remain ephemeral.** `handle_new_like()` still creates a normal expiring `matches` row for the product experience, and also writes a `venue_match_events` row keyed by match, venue, and night. Admin stats count the aggregate event so match efficiency survives cleanup without exposing who matched whom. *Why:* the discreet double opt-in object stays ephemeral for users, while founders retain the venue/night proof needed to understand whether the room is working.
- **Founder ejection is a night-scoped venue safety action, not a permanent account ban.** Admins can eject a reported/blocked profile from a specific venue for the current venue night. The RPC writes an audit row in `venue_ejections`, closes active presence, and `check_in()` refuses re-entry to that venue until the next night. *Why:* v1 moderation needs immediate in-room safety without building a heavy account-ban system; night-scoped ejection fits the live venue model and keeps enforcement proportional.
- **Blocks require a reason and optional note, because they are moderation signals.** Reports already carry structured reasons; blocks now do too, and the admin queue shows that context before a founder ejects someone. *Why:* a block is a user-protection action, but founder enforcement needs context and proportionality. Reasoned blocks keep the queue reviewable without forcing every user to file a full report.

## 2026-07-17

- **Founder profile preview is an explicit test override, not the default room model.** Venue ops can temporarily let checked-in users see completed public profiles when their normal live-room feed is empty; closing the room resets the override. *Why:* founders need to test the scroll feed with sparse attendance, but the product must still default to live, in-room discovery and avoid turning ordinary usage into a general profile browser.
- **Venue Intelligence moves to first-party aggregate analytics with small-cohort privacy suppression.** Existing business tables remain the source of truth for scans, profiles, check-ins, and messages; the new `analytics_events` table records only missing actions (landing/session/venue-open/discovery/profile-view/chat-open) with UTM/QR attribution, while `venue_conversation_events` persists message-count aggregates without message content. Gender/preference insights stay because they are useful for venue fit, but are hidden below a 10-person cohort. *Why:* founders need actionable acquisition, activation, conversation, retention, and venue-quality signals without inventing unsupported features, exposing raw users, or making sensitive claims from tiny samples.

## 2026-07-18

- **Venue Intelligence funnels use a single scanned-user cohort.** The conversion journey counts unique QR scanners, scanned users who created/completed profiles, and scanned users who checked in for the same venue night; raw attendance remains a separate check-in metric. *Why:* mixing total historical presence rows with scan/profile counts made impossible funnels, such as more entrants than profile creators. Founder analytics should compare one cohort through the journey and keep broader attendance as context, not as a conversion denominator.

## 2026-07-22

- **The repo now lives in a GitHub organization (`getamourette`), not a personal account, and is renamed `amourette-webapp`.** The repo was transferred from `AymaneSghier/qr-web-app` to `getamourette/qr-web-app` and then renamed; both founders are org owners; Vercel's GitHub App was granted access to the org and the project reconnected (env vars are on the Vercel project and were preserved; deploys track the repo by id, so the rename was transparent). GitHub keeps redirects from the old paths. *Why:* the whole company's code sat on one founder's personal account (a bus-factor and control risk), and GitHub refuses to link a Project board to a repo owned by a different account — an org fixes both at once. Pre-launch, with no prod users, was the cheapest moment to do it. A GitHub org (not a personal repo) is now the home for all company repos going forward.
- **The GitHub Project `Amourette` is the single shared source of truth for remaining work.** Board at https://github.com/orgs/getamourette/projects/1, linked to the repo, auto-adding new issues to `Inbox`. Fields: `Status` (Inbox → Backlog → Ready → In progress → In review → Done), `Kind`, `Area`, `Owner`, `Priority`. It holds *all* shared work, not just code — bugs, features, design, infra, and non-code ops (marketing/legal/business) via `Area: ops`, since there was previously no shared place for the latter. **Questions live on the board** (`Kind: question`) so they are shared rather than carried in one founder's head; a resolved question becomes a task or a decisions entry. Each founder's personal to-do tool (e.g. Marwane's Todoist) is demoted to private scratch that feeds the board but is never authoritative; `roadmap.md` stays the narrative layer and points at the board. The convention is written up in `AGENTS.md` (Task tracking). *Why:* two founders coding across two timezones need one place that is unambiguously current, or work silently forks; making the board authoritative (and forcing questions and ops onto it) is what stops either founder from carrying the backlog alone.
- **"Clear the other founder's open PRs before starting new work" is an explicit start-of-session ritual.** Reviewing/merging what is ready comes before opening new work, and is now baked into the `standup` skill; `ship` moves a shipped item to `In review` when its PR opens. *Why:* with a ~6h Paris/NY gap, a PR that sits unreviewed while the other founder builds on a moving `main` is exactly how avoidable conflicts and schema drift appear; the review is the guard-rail, not overhead. In the dev phase self-approve stays allowed so neither founder is ever blocked waiting.
- **Self-merge is allowed in the dev phase, except for PRs touching schema/migrations or the product invariants, which need the other founder's eyes before merge even at 0 users.** For ordinary work a founder may open and merge their own PR without waiting (review is encouraged but non-blocking, so nobody is ever stuck across the timezone gap). The exception is any PR that changes the database schema or a migration, or that touches an invariant (discreet double opt-in, no open DM, women-first safety, PII/RLS handling): those wait for the other founder's review before merge. *Why:* self-merge keeps a two-person async team moving, but schema/migration drift and invariant regressions are the two failure modes that are expensive or dangerous to unwind on a shared DB and a product whose whole trust model rests on those red lines, so they are worth the one blocking checkpoint even before there are real users.
- **Board ownership is the native GitHub `Assignee`, not the custom `Owner` field (which is removed).** The per-item owner is now set via GitHub's built-in assignee (`Marwane` = `marwaneazzouzi`, `Aymane` = `AymaneSghier`); the custom single-select `Owner` field was deleted from the board. This supersedes the same-day entry above that listed `Owner` among the fields. `AGENTS.md`, `docs/workflow.md`, and the `/task` and `/standup` skills are updated to read/write `Assignee`. *Why:* the native assignee renders each person's avatar directly on the card (which `Owner` did not), works on both issues and draft items, and gives GitHub-native notifications and "assigned to me" views — so keeping a second custom field alongside it was pure duplication that would drift. Setting it costs a different call than the single-selects (`gh issue edit --add-assignee` for issues, the `updateProjectV2DraftIssue` GraphQL mutation for drafts), documented in the `/task` skill.
- **A fourth skill, `/pick`, owns the "start a task" step, and each task gets its own git worktree.** `/pick` sits between `/standup` and `/ship`: it lists your pickable `Ready` items, converts a chosen draft to a real issue if needed, moves the card to `In progress`, assigns it to you, and cuts the `feature/…`/`fix/…` branch in a dedicated worktree (`<repo-root>--<slug>`), created with `.env.local` copied and deps installed (or reused if it exists). Because a running agent session cannot relocate into another directory, `/pick` ends by printing a ready-to-run launch command (`cd <worktree> && claude "…"` under Claude Code, `codex "…"` under Codex) whose prompt briefs a fresh agent with the task and its issue number, then has it read the issue and **wait for the founder** (to discuss the approach or enter plan mode) rather than build autonomously. `/pick` prepares the environment, it does not start the work. The worktree is created with `git worktree add --no-track` so the branch's upstream is not wrongly set to `origin/main` (which would mislead `/standup`'s drift detection and `git push`); `/ship` sets the real upstream at push time. *Why:* the contract already said "the agent moves `Ready` to `In progress` when it cuts the branch", but no skill executed it, and starting a task by hand (branch, worktree, env, deps, board move) was error-prone. One skill per lifecycle stage (`/task` capture, `/standup` session start, `/pick` task start, `/ship` end) makes the flow uniform across both founders and both agents. Worktree-per-task is what makes parallel agents safe: each task is an isolated checkout, so sessions never fight over the working tree; the only shared-state caveat is the single Supabase DB, so no two parallel tasks should touch the schema at once. Skills depend on `jq` (not installed by default; dropped a static binary into `~/.local/bin`). Two board-read gotchas were fixed across all three board skills at the same time: `gh project item-list` defaults to 30 items (smaller than the active board), so `standup` and `pick` now pass `--limit 500` or active cards fall silently out of view; and `/task` now resolves a freshly created issue's board item id via `gh project item-add --url` (idempotent, returns the id directly) instead of scanning `item-list`, which missed the item to both the 30-item limit and auto-add lag. Auto-archiving `Done` items to keep the active board small is deferred as a separate backlog task.
- **Phone testing standardizes on per-branch Vercel previews, and `DEV_LAN_ORIGIN` / LAN `next dev` testing is removed.** Testing a change on a phone now means opening the branch's Vercel preview at the deterministic URL `https://qr-web-app-git-<branch-slug>-tothe-moon.vercel.app` (the Vercel project is still named `qr-web-app`); `npm run preview:qr` (`scripts/preview-qr.mjs`) builds that URL from the current branch and renders it as a scannable terminal QR via the existing `qrcode` dependency. The `allowedDevOrigins`/`DEV_LAN_ORIGIN` machinery in `next.config.ts`, the `.env.example` entry, and the LAN paragraph in `AGENTS.md` are all removed. *Why:* the LAN path needed each founder's machine IP in `.env.local`, only worked on the same network, served plain HTTP, and could not be shared with the other founder. A Vercel preview is a stable HTTPS URL that exercises the real deployed build and QR flow and is shareable across the Paris/NY gap — better for a two-timezone team. Note for Aymane: this kills LAN `next dev` phone testing for both of us; use `npm run preview:qr` against your pushed branch instead.
- **Design-system rework (#38): the 07-03 "Rouge Signature" DA is being pressure-tested against real screens and is moving; the room feed card (hero #1) is locked on a new direction.** `design.md` turned out to be a one-shot generation, not a considered system, so solidifying it is part of #38. Marwane's call was to treat the 07-03 palette and fonts as **provisional** (dark stays, anchored to the bar-at-night context) and rebuild the visual language screen by screen by rendering comparable variants on the real product surface and choosing (mockups in `docs/brand/explorations/`, served via the existing `python3 -m http.server` LAN pattern). **Hero #1 locked:** the room card = full-bleed chiaroscuro ("Sous les projecteurs"), first name in **Fraunces italic**, cool **ink** ground `#120A0F`, deep signature red `#CC1436` used **present** (filled ♥ at rest), one **champagne 1px hairline** in the footer. Emerging shifts vs 07-03: Bodoni→Fraunces (display/wordmark), Inter→Figtree (body), Jost kept (labels); palette cooler/deeper (velvet→ink, ruby→deep red). The **full token table is rewritten only after the second hero (the match reveal)**, so the spacing/radius/type scales come from two screens, not one. *Why:* deciding on rendered product screens beats debating tokens in the abstract, and the 07-03 doc was never a real system. This **partially supersedes** the 07-03 (Direction A) and 07-04 (Bodoni wordmark + velvet/ruby implementation) entries; those stand as the record of what was considered. **Open point carried to the reveal:** a permanent red ♥ ("red present") softens the 07-03 "red is an event" rule and must be validated so it does not dilute the full-red reveal. The **wordmark/logo is a separate later pass** (reopens the Bodoni wordmark; the unused "wax seal" mock idea is a strong app-icon candidate). **Heads-up for Aymane:** the shipped app UI still shows the 07-04 Bodoni/velvet/ruby look — the new direction is mockups only until the refonte lands, so expect a transient mismatch.

## 2026-07-23

- **Worktree cleanup requires an exact merged-PR match and a non-active board item; Git state alone is never proof of completion.** `/standup` may classify a branch as safely deletable only when its worktree is clean, a merged GitHub PR has that exact branch as `headRefName`, and the linked board item is no longer `In progress`; it then shows that evidence and asks for confirmation. A branch that merely points at a commit in `main`, has a gone upstream, or has no local changes is preserved as ambiguous when the PR or board evidence is missing or contradictory. *Why:* a freshly prepared worktree can be clean and point at `main` before its first change, while squash merges make ancestry unreliable in the opposite direction. Treating those Git heuristics as proof caused the unfinished `feature/logo-wordmark-app` worktree to be deleted even though its board item was still active and no matching PR had merged.

- **`npm run preview:qr` opens the permanent crowded QA venue by default and accepts named venue/branch options.** The generated Vercel preview URL now includes `/v/test-crowded`; `--venue <slug>` selects another room and `--branch <name>` selects another preview branch. *Why:* the phone-testing helper should land directly in a useful, stable room with zero required arguments, while named options avoid ambiguity once both venue and branch are configurable.

- **A draft PR is shared WIP; only Ready for review means the work is complete and mergeable.** A branch may be pushed without a PR for a Vercel preview/checkpoint, or with a draft PR when shared testing and early feedback are useful; both states remain `In progress`, and draft PRs must not be merged. `/ship` is the explicit final-delivery signal: it runs the full lint+build gate, pushes, creates or updates the PR, marks it Ready for review, verifies that GitHub state, and only then moves the board card to `In review`. Contextual preview/checkpoint requests still push testable WIP with proportionate checks and optionally open/update a draft PR. `/standup` separates ready PRs requiring action from draft PRs shown only as active WIP. Ready is a prerequisite for every merge but does not change the existing self-merge rule or its schema/invariant exceptions. *Why:* an open or draft PR previously meant both "please test this" and "please review/merge this", so founders and agents could not tell whether work was complete; making GitHub's draft/ready state authoritative preserves fast Vercel testing while giving the async Paris/New York workflow one unambiguous review and board signal.

- **Design-system foundation (#38, Étape 0): shadcn/ui adopted on Tailwind 4, v2 tokens and fonts wired, WCAG re-measured.** `shadcn init` (base `radix`, style `radix-nova`) added `components.json`, `lib/utils.ts` (`cn()`), a first `components/ui/button.tsx`, and the deps (`class-variance-authority`, `clsx`, `tailwind-merge`, `radix-ui`, `lucide-react`, `tw-animate-css`); the `shadcn` CLI package itself is a `devDependency` (it is only a build-time `@import "shadcn/tailwind.css"` and the CLI). The v2 palette values from `design.md` are wired into `@theme` in `app/globals.css`, and shadcn's semantic tokens are mapped onto the Amourette palette so shadcn primitives read as Amourette out of the box: `--primary` = `red` / `--primary-foreground` = `cream`; `--card`/`--popover`/`--secondary`/`--muted` = `bordeaux`; `--muted-foreground` = `taupe`; `--accent` = `wine`; `--ring` = `blush` (the focus ring); `--destructive` = `blush` on `ink` (red is love, never danger, so there is no alert red, safety stays blush per the DA); `--border`/`--input` = a faint cream neutral, never champagne, so gold stays precious (one hairline per screen); `--radius: 1.25rem`, which with shadcn's calc multipliers lands exactly on the design radius scale (sm 12, md 16, lg 20, xl 28). Because the app is dark-only (the night is the set), `:root` carries the dark values directly and no `.dark` class is required. Fonts swapped in `app/layout.tsx` via `next/font`: Bodoni to **Fraunces** (display/wordmark/reveal, italic is the brand voice), Inter to **Figtree** (body), Jost kept (labels); `--font-sans` points at Figtree so shadcn primitives inherit the body face (the `@theme inline` self-referential `--font-sans: var(--font-sans)` that `init` writes is a known Tailwind 4 gotcha and was removed). WCAG re-measured for the v2 values: all pass (cream-on-red 4.59:1, red-on-velvet 3.46:1, red-on-bordeaux 3.29:1), closing design open point (1); cream-on-red at 4.59 is the floor, so no text under 12px on a red fill. Validated on a throwaway `design-preview` route (not committed) rendered with the real fonts and compiled tokens. *Why:* the branch (`feature/design-system-shadcn`) commits to shadcn as the component layer, and doing the plumbing (init, token mapping, fonts, contrast) as one non-visual-breaking foundation PR lets the hero rebuilds that follow just consume the system instead of re-deriving it. Mapping shadcn's tokens onto our palette rather than keeping two parallel systems is what keeps standard components (dialogs, inputs, dropdowns) on-brand for free. One gotcha found and fixed: shadcn's generated `button.tsx` imports `Slot` from the unified `radix-ui` barrel (which calls `createContext`) but shipped without a `"use client"` directive, so it 500s in a Next 16 Server Component; the directive was added. The heart's idle/liked classes were updated to v2 values but keep the superseded 07-03 idle-outline behaviour for now; the flip to "red present" lands with the room-card hero rebuild, to keep Étape 0 from changing the existing screens.
- **Room feed card (hero #1, #69) built for real: full-bleed confirmed over a card frame, count relocated to the header, legibility solved by guaranteed scrims + a global night grade, and "red is an event" confirmed (the ♥ is discreet at rest, red only on tap).** A build session with Marwane (exploration on a scratch route, `app/room-hero-lab/`) froze the execution and it was ported into `app/v/[venueSlug]/page.tsx` as a presentational `RoomFeedCard`. The decisions that closed: (1) **full-bleed is the direction, the card-frame variant is rejected** (it was only built to compare against and reopened the locked "no card frame" rule); (2) the **room count moves to the on-photo header** (red live-dot + "N in the room", new compact `liveStatus` string in all three locales) and appears exactly once, killing the old kicker/near-heart redundancy; (3) **legibility is two-layered** so text never sits on a raw photo: guaranteed local velvet scrims where text lives (top header fade + bottom scrim to ~98% velvet), plus a **global "night grade"** (a warm velvet multiply veil over every photo, `.room-grade`) that crushes highlights and neutralises pure white and bright gold, pulling every face into the same venue darkness ("the night is the set") and guaranteeing a bright selfie stays readable, stress-tested against a synthetic white/gold photo (Marwane validated the grade on the preview); (4) the **"red present" ♥ tried in the rebuild was dropped on the real screen and the tension resolved in favour of "red is an event"**: a filled-red ♥ at rest read as pre-selected/on and diluted the accent, so the resting state is a discreet ghost pill (semi-velvet + blur, cream label, ♡) and it turns red (♥ fill + one-shot bloom) only when tapped — red stays reserved for the like action and the full-red match reveal; `design.md` updated accordingly. States/motion added beyond the resting mock: hover, blush focus ring, press `scale(0.97)`, an Expo.out identity fade-in, `prefers-reduced-motion`. Two integration calls: the app already has a persistent sticky header carrying the wordmark + venue, so those are **not** duplicated into each card (only the live status + per-profile safety `⋯` sit on the photo) to avoid a wordmark stutter on every snap; and `justArrived` became a small kicker above the name. *Why:* the mock was a directional proof (resting-state, placeholder photo); the real jump is real photos + the night grade + states, and the grade is what turns the biggest legibility risk (a bright user photo under cream text) into a guarantee rather than a hope. Seeing it live is also what flipped the ♥ decision, which is exactly why we build on the real surface before locking pixels. Follow-up surfaced by the review and carried to a separate task: the persistent room-screen chrome (header, "Tonight at <venue>" copy + long-name handling, the two stacked ⋯ menus with the room menu not closing on outside-click, the own-profile avatar, and the matches strip pushing the full-bleed photo down) needs its own refonte to sit right under a full-bleed card; and the wordmark/logo pass stays open (font + per-context colour). Tunables validated: night-grade opacity kept, "Tap" kept for now. The scratch route stays in the branch only so the card is previewable on a phone without auth, and is deleted before merge.
- **The v1 email capture is an optional global Amourette-night subscription, not an account upgrade.** After two minutes of active, foreground room use, an unsubscribed user may see a dismissible email modal with a dedicated unchecked marketing-consent box. Dismissing suppresses it for the venue night (the same 06:00-local boundary as presence); a later night may ask again. The owner-only `profile_private` row stores the normalized contact plus consent time/version, while public auth stays anonymous and device-bound. Delivery, verification, and unsubscribe management are separate board tasks (#63 and #64); the unresolved permanent-account choice stays a board question. *Why:* the room must deliver value before asking for a retention channel, marketing consent must stay explicit and separate from authentication, and collecting privately now must not silently expand v1 into login/recovery or weaken the zero-friction QR funnel.

- **Development QA uses two explicitly marked, rollover-exempt venues: one deterministically crowded and one empty.** `venues.is_test_venue` separates QA data from real venue operations and founder analytics; `rollover_disabled` is constrained so it can only be enabled for a test venue. The 06:00 job preserves presence, likes, matches, and liveness in those rooms, while a manual admin stop still works normally. An idempotent service-role CLI owns 36 synthetic Auth-backed profiles and may pre-like a supplied tester profile to exercise the real reciprocal-match path; it refuses to clear venues not marked as test. *Why:* scroll, waiting, realtime, and match behavior need stable preview/local fixtures without manufacturing a real night, while explicit database markers and venue-scoped cleanup prevent test behavior or metrics from leaking into real venues.
- **Anonymous browser sessions persist across venues; presence, not identity, is venue-bound.** Scanning a different venue's QR keeps the same anonymous Supabase session and permanent profile, while `check_in()` closes any active presence in the previous venue before opening one in the new venue. Likes, matches, blocks, reports, and room state remain scoped by `venue_id`. This supersedes the 2026-07-09 decision to sign out and create a new anonymous user on every venue change. Persistence is still device-bound until Amourette deliberately adds account recovery. *Why:* a person is the same person across nights and venues; rotating `auth.uid()` orphaned their profile and forced onboarding again, while the existing presence model already enforces one live room at a time without sacrificing persistent identity.
- **Room screen chrome refonte (#83, follow-up to #69): the persistent chrome floats over the full-bleed feed instead of a sticky bar, one context-aware menu replaces the two stacked `⋯`, and the venue copy is reworked.** Explored variant-by-variant on the real surface (a scratch `app/room-chrome-lab/` route, deleted before merge, like `room-hero-lab`) before porting into `app/v/[venueSlug]/page.tsx`. The calls that closed: (1) **wordmark-led, not venue-led or a slim bar** — the venue-led layout read best but "Amourette" anchors the top-left for brand recall (people must remember the app), with the venue name and live count beneath it; the slim persistent bar was dropped as less clean. (2) **The old sticky header is gone**: it was a `shrink-0` bar that grew when a match existed and visibly shrank the full-bleed photo. All chrome (brand, venue, live count, menu, matches) now floats as overlays on the feed, so the photo is always full-screen and nothing pushes it. (3) **One context-aware `⋯`, top-right**, kills the two-stacked-menu problem (room `⋯` + per-profile safety `⋯`): because the feed is one profile per viewport, the single menu carries the visible person's Report/Block (tracked via feed scroll → `currentVisibleId`) on top, then the room/self actions (edit profile, language, invisible, leave). Safety stays exactly two taps, so women-first reachability does not regress. The old outside-click bug is fixed by construction (backdrop `z-40` under panel `z-50`, no sticky bar to sit behind). (4) **Venue copy reworked**: "Tonight at <venue>" (`whosHere`, now removed) becomes the venue name on its own truncating line + a red-dot "N here now" live count on the line below (`liveStatus` recopied to "here now / sur place / aquí ahora"), so a long venue name never eats the count. (5) **Matches are a collapsed pill** (overlapping avatars + "N matches" + total-unread count) that expands to the full strip and collapses on outside tap — a match is precious, a small persistent signal beats a permanent strip eating the photo; the unread *count* beats a bare dot because it is actionable. (6) **Own-profile avatar folded into the menu** ("Edit my profile") rather than a standalone top-right element, keeping the top clean. *Why:* the #69 card locked full-bleed as the direction, but the surrounding chrome was still a sticky bar built for a card-framed feed; it duplicated brand context, stacked two menus, and shrank the very photo the card direction fought for. Building the options on the real surface (not a mock) is what let Marwane pick wordmark-led over the prettier venue-led and confirm the pill over the strip.

- **Match reveal (hero #2, #70) direction locked: overlapping portraits ("chevauchement nu", mock variant C) with a fine champagne ring on the front portrait, and the reveal is the one screen allowed two champagne touches.** Built variants on the real surface (previews under `public/_explore/`, temporary, removed before the component PR); Marwane picked variant C, then a second round on the front-portrait edge treatment (champagne / cream halo / bare depth / blush) to replace the muddy red-on-red ring from the mock. **Calls that closed:** (1) the front (the *match's*) portrait carries a **1px champagne ring**, not the mock's 3px red — red-on-red-deep read as low-contrast and unfinished; the back portrait (you) recedes with a faint cream edge and a whisper of velvet, so overlap + light (not a red stroke) says "her, in front". (2) A low **champagne "spark"** glow rises where the two faces meet — a curtain of light, not confetti (the emotional lift Marwane liked in variant C). (3) **The match reveal is the sanctioned exception to "one champagne per screen":** it may carry two champagne touches (the portrait ring + the 60px hairline) because it is the signature full-red moment; gold stays banned as a *fill* everywhere, and every other screen keeps the single-hairline rule. This closes the #38 open point carried from hero #1: the drama lives in the reveal (full red + chiaroscuro + the ring/spark), so the room card does **not** need a standing red to exist — "red is an event" holds, and the reveal is the event. States/motion added beyond the mock: the screen rises like a curtain (fade + translate, Expo.out `cubic-bezier(0.16,1,0.3,1)`), the portraits settle from `scale(0.94)`, the spark blooms once, CTAs press `scale(0.97)`, blush focus rings, `prefers-reduced-motion` keeps the state and drops the motion; the empty/missing-photo state reuses `ProfilePhoto`'s initial fallback. Component built inline in `app/v/[venueSlug]/page.tsx` (replacing the placeholder reveal), reusing the local `ProfilePhoto` and the current user's profile (`me`) for the back portrait. *Why:* the mock was a low-fi directional proof (red-on-red ring, single portrait, no states/motion); the real jump is real overlapping photos + a light-based separation + real motion, and building on the real surface is what surfaced that the red ring was the weak part. The champagne-exception is the honest way to reconcile Marwane's ring choice with the DA's single-gold rule without weakening it elsewhere.

- **Unlike is an immediate second tap before a match; closing an established match remains a separate action.** The filled heart stays actionable and removes only the current user's active outgoing like, with no confirmation or signal to the other person. If a reciprocal like creates a match concurrently, the match wins and remains available; unlike never deletes a match, chat, or message. *Why:* correcting a private tap should stay lightweight and reversible, while withdrawing an already-open mutual conversation has visible consequences for another person and needs its own deliberate women-first UX.
- **Onboarding + profile edit refonte (#72): profile creation becomes a guided one-question-per-screen wizard ending on an editable room-card preview; editing stays a single screen; both share field widgets; progress persists client-side.** Explored three directions on the real surface (`docs/brand/explorations/onboarding-directions.html`, served via the LAN `python3 -m http.server` pattern): A "le salon" (one refined panel), B "une question à la fois" (guided steps), C "ton portrait" (chiaroscuro hero preview). Marwane picked **B for the flow + C as the final confirm screen**. The calls that closed: (1) **Creation is a guided wizard** — Prénom → Photo → Je suis → Je veux rencontrer → Bio → **Confirme** (five questions, bio its own optional/skippable step so it shows big in the preview). The confirm screen is the C direction as a **clean showcase**: your photo full-bleed with the room-card chiaroscuro treatment, name + bio + chips over the scrim, and the 18+ confirm as the single control at the moment of entry (a calm blush note above the CTA — the legal gate lives where you enter, not scattered mid-flow). It is the **single write to the DB** (the `profiles` insert + `profile_private` age-confirm happen only on "Entrer dans la salle"), so there is never a partial row in the shared DB. The **language selector shows only on the first screen** (the locale is a one-time entry choice) instead of floating over every immersive screen, where it overlapped the content. (2) **Editing does NOT walk the wizard** — a returning user changing their bio gets a single editable screen (`ProfileEditor`), and the age gate is not re-asked (`profile_private` untouched). (3) **Creation and edit share the same field widgets** (`app/profile/fields.tsx`: `PhotoPicker`, `Segmented`, `AgeGate`) so behaviour/validation/DA can never drift; the only per-context difference is a `layout`/`size` prop (stacked Fraunces rows in the wizard, compact pills in the editor). (4) **Interrupted onboarding resumes** — the scalar answers (first name, bio, gender, interested_in, 18+, step index) persist to `localStorage` keyed by the anon user id and restore on return; the **photo is deliberately not persisted** (a `File` does not serialize), so on a full tab close it is re-asked and the flow clamps back to the photo step. Persisting the photo Blob to IndexedDB is the tracked follow-up **#98**. No DB migration — the data model is unchanged; all persistence is client-side. Motion per the v2 system (Expo.out step fades, press `scale(0.97)`, `prefers-reduced-motion`); focus rings blush, safety never red. New i18n keys under `profile.onb` (FR/EN/ES). *Why:* Marwane wants onboarding to guide the user (B), and ending on a live preview of your room card (C) ties the first-run to the product's signature surface while keeping the DB write atomic. Sharing field widgets is the guard-rail against creation and editing silently forking. Client-side drafts avoid partial rows and a schema change while still surviving the realistic "checked a message mid-flow" interruption; the photo is the one thing that cannot cheaply survive a full close, so it is scoped out to #98 rather than blocking the refonte.

## 2026-07-24

- **Match chat surface (#73) reworked to the v2 system, direction "B calme": a presence-led header, sober bubbles (no warm colour), and one soft opener carrying the ephemeral.** Explored three directions on the real surface via a temporary `/chat-preview` route (A "le chat s'efface" / B "in-person first" / C "couture"; `docs/brand/explorations/chat-directions.html` is the first-round proof), then converged with Marwane on **B, dialled down**. The calls that closed: (1) **one presence signal, calm** — a red live-dot + "In the room" (`presence`) in the header; "maintenant/now" was dropped because the live-dot already says it, and the repetition read as forced urgency. (2) **The ephemeral is said once, softly, in a pinned opener** at the top of the thread ("You both tapped." `openerTitle` + "Over a drink" `openerNote`), **not** as a permanent banner and **not** as a popup — a banner is noise and a popup forces/interrupts (against discreet + women-first calm); "soft urgency" means whispered once, then it scrolls away. A real contextual nudge (near closing, or when her presence drops) is deferred as a signal-driven follow-up, not decorative. (3) **Bubbles stay sober** — mine on `bordeaux-warm`, received on `bordeaux-deep`, both cream text, no red/wine fill: the warm-bubble variant (C) was rejected because a coloured "my" bubble on every message dilutes "red is an event" (the like + the reveal). (4) **Timestamps on both sides**, under each bubble (Jost, taupe). (5) **Safety folds into a single `⋯` menu** (Report/Block in blush, never red, + language), keeping the header calm; it closes on any outside tap via a `pointerdown` listener rather than a backdrop div, because the header's `backdrop-blur` makes a `fixed` overlay resolve against the header box, not the viewport. (6) **Ambient ground** so the surface reads as a bar at night, never a flat fill: a warm ember rising from the composer + a wine glow up top + an edge vignette + a whisper of grain, no second hue (discretion stays). States/motion: loading, error, closed/expired, empty, sending (disabled), send failure, typing, report/block modals; opener and messages fade in with `animate-curtain` (Expo.out), send presses `scale(0.97)`, `prefers-reduced-motion` respected. All new copy is in `lib/strings.ts` for the three locales. The realtime/typing/read-marker/i18n logic is unchanged — this was a visual rework. *Why:* the chat is a trigger to talk IRL, not a place to live in, so the surface's job is to stay calm, say "she's here, tonight" exactly once, and get out of the way; showing three directions on the real surface is what let Marwane pick B and then strip its urgency down to one presence line and one opener.

- **iOS Safari's floating bottom bar is cleared with a `visualViewport`-measured height plus explicit composer clearance, and `viewport-fit=cover` is now set app-wide.** The chat is a single non-scrolling page (only the thread scrolls) sized to `window.visualViewport.height` (falling back to `100dvh`), with body scroll locked while mounted. Because iOS 26 Safari's floating bar overlays CSS-viewport content without shrinking any of `vh`/`svh`/`dvh` (each of those was tried and left the composer tucked under the bar), the composer also carries `padding-bottom: calc(env(safe-area-inset-bottom) + 2.5rem)` to sit fully above it, and a root `viewport` export sets `viewportFit: "cover"` so `env(safe-area-inset-*)` is populated at all. *Why:* a bottom-pinned composer that is half-hidden by the browser chrome is unusable, and the height units that normally solve this do not account for the floating bar; measuring the real visible viewport in JS and reserving the bar's space explicitly is the only combination that held on the device. `viewport-fit=cover` is a global change but the correct default for a mobile-first full-bleed dark app (it also lets the room/reveal pad around the notch); watch for other screens needing safe-area padding as a result.

- **Room-entry redesign (#103): make the existing loading/transition a designed "threshold", not a hard opt-in entry gate.** We considered inserting a real entry gate between the QR scan and the live feed (venue context + "who's here" + an explicit "Enter the room" button, with `check_in` firing on the tap instead of on load). We rejected it for v1. *Why:* a hard gate fights the north star (scan → see who's here, *immediate*) by adding a tap before the payoff, and it is only worth building if it shows live proof ("N here tonight") — which it cannot, because `presence` SELECT is RLS-scoped to venues you are *already* checked into (decisions 2026-06-19), so a pre-check-in count would need a new `SECURITY DEFINER` count RPC + grants + advisors, i.e. backend work for a screen whose main value is a consent moment. The women-first agency value of a gate (seeing the room before you appear in it) is captured ~80% for free by redesigning the `loading` state: the async `check_in` already runs during it, so instead of a bare spinner we make it a warm "you're walking into [venue]" threshold that auto-advances to the feed. The hard opt-in gate is kept in reserve for a future explicit consent moment; if we build it we will add the definer count RPC then. Scope for #103 stays UI-only (no migration, no check-in timing change).

- **The entry threshold (#103) is an arrival ceremony, not a loading spinner: held on a real arrival, quiet on a detour, replayed after leaving.** The redesigned `loading` doorway flashed as an unreadable "stamp" whenever the room re-booted fast (bouncing back from the profile editor lands you in `loading` for ~300ms). Rather than always showing it or always hiding it, we split the two journeys with a per-tab `sessionStorage` marker (`amourette-entered:<slug>`): the FIRST entry of a session plays the doorway in full and holds it a readable minimum (`ARRIVAL_MIN_MS`, 2.2s, measured from mount so a fast load still reads as a threshold); an incidental re-entry (profile-edit detour, re-boot, foreground return) renders only the bare ambient night, no copy. Explicitly *leaving* the room (`leave()` sets `presence.left_at`, you disappear from it) clears the marker, so returning by the "re-join" button or by re-scanning the QR both re-cross the threshold and replay the held doorway; going invisible→visible is not an arrival and stays instant. *Why:* the doorway's value is the arrival moment (naming the venue, the live-dot, the discreet-like reassurance), which only makes sense when you actually cross into the room; replaying it on every in-app detour turns a ceremony into an annoying flash, and never showing it makes a fast arrival unreadable. Leaving is the one re-entry that is a true re-arrival, so it alone resets the ceremony. `ARRIVAL_MIN_MS` is a single named constant, easy to tune once felt on-device.

- **Landing page (`/`, #71) reworked to the v2 system, direction C ("Cérémonie"): a centred ceremonial front door, not a funnel; the wordmark is red here as a scoped exception; the cold-email waitlist is UI-only pending its own store (#105).** Rendered three splash directions on the real surface (`docs/brand/explorations/landing-directions.html`): A "éditorial" (bottom-left, red = the action), B "le seuil" (full-bleed photographic), C "cérémonie" (centred, red wordmark). **B was rejected on principle**: it copies the in-venue room-card treatment, so on the off-venue landing it sets the wrong expectation ("you're already in the feed") when the whole point is "go scan at the bar"; Marwane picked **C**. The calls that closed: (1) **The landing is a gate, not a funnel.** Off-venue the real action (scan a QR at the bar) is not clickable, so the page reads as an *affiche* — promise + mood + a clear IRL instruction — not a conversion funnel; the only clickables are the language selector and, for returning users, edit/reopen-chat. All three gate states (`loading` / `new` / `returning`) are reworked to v2, keeping the existing `2026-07-01` new-vs-returning gate logic. (2) **New rule — the wordmark is red on the landing (all gate states), cream in-app.** The landing is the brand's public threshold, so red is the *identity* here; everywhere inside the app the wordmark stays cream and red returns to being only the like / primary CTA / reveal. This is how direction C's red wordmark is reconciled with "red is an event" without reopening it: the front door is the one place a standing red is the identity, not a resting UI accent. Provisional until the wordmark/logo pass (#38). (3) **`new` splash** = kicker ("The bar · tonight") + red Fraunces-italic wordmark + promise line ("The people in this bar, without the fear of the first move.") + one champagne hairline + a three-beat how-it-works (Scan at the door · Like in secret · Match to talk) + the discreet waitlist. (4) **`returning` = a calm "home of identity"**, not an app dashboard: a centred profile card (photo, name, edit) + any still-live match chats as **`bordeaux-warm` rows, not red buttons** (red stays the wordmark + reserved accents) + the soft "scan your bar's QR" reminder. `loading` is just the red wordmark breathing (a candle swell, no spinner). (5) **Cold-email waitlist split.** The landing new-visitor has no `profiles` row (anonymous, device-bound), so their address cannot live in `profile_private` (FK to `profiles`) the way the in-room capture does (#33/#76); it needs a standalone **`email_signups`** table (#105, infra) as the profile-less entry point of the *same* global subscription — #63 delivery reads both sources, #64 unsubscribe covers both. In #71 the `WaitlistForm` UI and all four states (idle/loading/success/error, errors in blush never red) are built, but `lib/waitlist.ts#subscribeEmail` is a **marked stub (`TODO(#105)`)**: submitting shows success but persists nothing until the table lands (pre-launch, no traffic; the swap is one line). Motion per v2 (`.landing-enter` fade Expo.out, `.breathe` loading, press `scale(0.97)`, `prefers-reduced-motion`); `LanguageSelector` restyled off its old gold look onto the v2 pill. New i18n keys under `landing` (EN/FR/ES). Verified on the real surface at a true 390 viewport (CDP `Emulation.setDeviceMetricsOverride` — headless Chrome silently clamps `--window-size` to a ~500px minimum, which faked a mobile-overflow that did not exist). *Why:* the landing is the brand's first impression off-venue but not the entry path (scanning is), so it must sell + explain + point to the bar without pretending to be a funnel; rendering three directions live is what let Marwane reject the room-card-lookalike and pick the ceremonial one. The email split keeps the shipped in-room capture untouched while giving cold signups a home in the same downstream pipeline. Acknowledged and accepted for v1: auth stays anonymous and device-bound (per `2026-07-16`), so a user on a new device is treated as new — the email-as-persistent-identity recovery that #33 gestured at is still not wired, and the collected address today only feeds night announcements, not reconnection.

- **Room popups (#111) unified on a shared `<Modal>` shell, and blocking is deliberately lightened while reporting keeps its full form.** The four popups around the live room (entry primer, email signup, report, block) were four inline overlays in `app/v/[venueSlug]/page.tsx` that had drifted: the email prompt was the only polished one, the entry hint was a thin in-room banner, and report/block were near-identical raw `<select>`+`<textarea>` forms with no hierarchy between them. We extracted a single presentational `components/ui/modal.tsx` (backdrop, Esc + backdrop dismissal gated by a `dismissable` prop, `night-panel` surface, optional `×`, `aria-labelledby`) and moved all four onto it, so structure/spacing/dismissal are consistent by construction — the first real modal abstraction, pulled now that four callers exist (not preemptively). Two product calls closed with Marwane: (1) **the entry hint is promoted to a real first-entry modal** (still one-time, `localStorage`-gated) landing right after the arrival ceremony, because a crisp double-opt-in primer ("no one is ever told they were tapped; a chat opens only if you both tap") is worth one blocking beat and reinforces invariant #3, where the old banner read as thin. (2) **Blocking is made near-frictionless**: the modal *is* the confirmation now (the native `window.confirm` is gone), and the reason — a moderation signal, not a gate — is folded behind an optional "add a reason" disclosure with a valid default (`unsafe_behavior`) already set, so a block is two taps and asks nothing. **Reporting keeps its reason+note form** (it feeds moderation) and its "also block?" follow-up. *Why:* invariant #5 (women first) says a safety control must be immediate; forcing a reason `<select>` + `other`-required note before someone can protect themselves is friction on exactly the wrong action, while the report path is where the structured signal belongs. The `blocks.reason` column is `NOT NULL` with a check constraint, so the default-reason-on-insert keeps the moderation signal and schema intact with no migration. Safety UI stays never-red per the DA (block submit is `night-button-danger` = blush on bordeaux; report submit is the cream affirmative); all copy is in `lib/strings.ts` (EN/FR/ES). One incidental fix: the email prompt's stale-venue render guard read a ref's `.current` during render (a latent `react-hooks/refs` violation the refactor surfaced), now mirrored into state (`emailPromptVenueSlug`) set in lockstep with the ref, behaviour identical. The chat page's own block flow (out of scope) still uses the native `window.confirm` + `blockConfirm` string, left untouched.

- **Edit-profile UI redesign (#102): grouped two-block layout, an explicit photo-edit affordance, and an unsaved-changes guard; interactive crop stays scoped to #31.** The single-screen editor (`app/profile/ProfileEditor.tsx`) was a flat, vertically-centred stack where photo, name, bio, and both preference selectors read at equal weight. Reworked within the existing v2 system (no DA migration): (1) content is **top-anchored** instead of `justify-center`, which on a near-full-height form fought the mobile keyboard and could push the save button off-screen; (2) fields are grouped into **two `night-panel` blocks** with `night-kicker` headings, "You" (photo, name, bio, "I am" — identity) and "I want to meet" (the interest selector), so the screen reads as identity-then-preference instead of one list; (3) the **Save + Back actions sit in the normal flow** at the bottom of the form (a first sticky/fixed bottom bar was dropped after on-device testing — on a short form it floated and overlapped the content); (4) an **unsaved-changes guard** — the shared `PhotoPicker` gained an `editable` mode (an explicit "Change photo" button with a pencil icon below the current image; a first on-image overlay was dropped as too subtle on device; default off so onboarding's photo step is visually unchanged), and leaving via Back opens a styled `night-panel` confirm overlay (matching the chat report-dialog pattern, not native `window.confirm`) plus a `beforeunload` listener; dirtiness is derived in `page.tsx` from a baseline snapshot captured on load (field diffs + a newly-picked photo file, interests compared as a set). New i18n keys (`youSection` + discard-dialog) in EN/FR/ES; `iWantToMeet` and `onb.changePhoto` reused rather than duplicated. **Scope split with #31**: #102 delivers the *affordance* (make the photo clearly editable), while interactive crop/resize stays #31, which will later swap the picker's `onChange` to open a cropper. Per-field inline validation was deliberately **not** built — the form is four fields, so the single (better-placed) message line is enough; YAGNI. No DB migration, no schema change. *Why:* the editor lagged the rest of the product's v2 polish and had two real UX holes the issue called out (no photo-edit affordance, silent discard on Back); grouping + a leave-guard fix those without over-building, and keeping crop in #31 avoids doing work that ticket owns.

- **Venue liveness becomes a durable, scheduled `venue_nights` lifecycle with `closed → waiting → live`, not a daily venue boolean.** Each explicitly scheduled night owns its waiting-open time, guaranteed launch time, close time, launch threshold (four participants by default), current status, and actual launch time. `waiting` permits QR entry, anonymous onboarding, and night-scoped presence while keeping every other participant profile hidden; the night moves atomically to `live` when the threshold is reached, the guaranteed time arrives, or a founder launches it manually, and never falls back to waiting when attendance drops. The admin configures and operates individual nights in the venue timezone, and every open waiting page follows the state change through Realtime with polling/foreground recovery. Waiting copy promises the actual configured deadline (for example, “Opening by 21:00 at the latest”) and explains that the room may open earlier as it fills. Implementation is tracked by #123–#125; the later room-launch push is tracked separately by #121. *Why:* real venues will not host Amourette every day, so putting recurring launch fields directly on `venues` would encode a false operating model. A per-night record gives scheduling, overrides, auditability, and one authoritative state machine, while the hybrid threshold-or-deadline rule protects initial density without making early participants wait indefinitely.

- **Venue-night closure has two deliberately different meanings: manual close is reversible; cancellation and scheduled end are terminal.** A manual close immediately hides the room and match/chat access and ends active presence, but preserves that night’s likes, matches, and messages. A founder may reopen it only before `closes_at`; everyone must check in again, and a previously launched night resumes `live` rather than returning to `waiting`. Cancellation or the scheduled close permanently expires the night’s ephemeral interactions and ejections, and can never be reopened. All transitions share one row-locked primitive and append one admin-only audit event; `venues.is_live` is now only its temporary compatibility mirror. Eligibility for threshold launch means a complete, adult-confirmed profile with active presence in that exact night; invisible users count because visibility is a discovery control, not a statement that they left the venue. QA venues follow the same engine through explicit live nights ending in year 9999, with no rollover bypass in runtime code. *Why:* founders need an operational pause/recovery control without destroying conversations, while the configured end must still enforce ephemeral presence and identity. Requiring fresh check-ins after a pause prevents stale attendance from silently repopulating a reopened room; keeping launched nights launched makes the lifecycle monotonic and race-safe.

- **Amourette will pursue an optional installable PWA with live-night Web Push, offered from the waiting room after profile creation; anonymous web entry remains the default.** Installation is never a gate: users may keep the waiting page open and receive the existing Realtime transition, while those who install can put their phone away and be notified when the room launches, when they mutually match, and when a matched chat receives a useful new message. The work is deliberately staged under #113: first prove on physical iPhones that Safari → Home Screen installation preserves the exact same anonymous Supabase identity and venue-night state (#119), then build the installable PWA and waiting-room journey (#120), subscription/delivery infrastructure (#122), and event experiences (#121). Cookie-backed web auth is the preferred continuity direction because it also supports the later optional conversion of the same anonymous `auth.uid()` to email/Google/Apple, but the proof remains a go/no-go gate before broader iOS push investment. V1 does not add a “save profile” account step; the existing marketing email remains separate from authentication. A future native app will reuse the permanent identity layer rather than require a second profile, while the zero-friction QR webapp remains available for first-time bar users. *Why:* the waiting interval creates a concrete reason to install — put the phone away without missing the launch — and match/message alerts reinforce the live, in-person loop, but neither benefit justifies forcing installation or making users recreate a profile. Sequencing continuity first prevents building a notification system on top of an iOS handoff that would silently split one person into two accounts.

## 2026-07-28

- **The balance-gated manual-launch experiment and event-control venue popup are reverted; the previously approved venue configuration popup and threshold auto-launch are restored.** Check-in once again launches a waiting night when eligible attendance reaches its configured threshold, founders retain the existing manual launch control, and guaranteed-time launch remains automatic. The temporary configurable venue balance field and readiness UI are removed. *Why:* after trying both iterations in context, the added launch decision system made the founder workflow more complicated than the operational value it provided; returning to the last accepted experience preserves the admin dashboard’s simplicity.*

- **Attendance and venue balance are readiness gates for founder-controlled early launch, not automatic launch triggers; guaranteed launch remains unconditional and automatic.** Each venue owns a configurable minimum women percentage, defaulting to 40%. Readiness requires the night’s eligible active attendance to reach its participant threshold and women to represent at least that configured percentage of the women-plus-men cohort; non-binary participants count toward attendance but are excluded from that denominator. Check-in no longer transitions a waiting room to live. Before the guaranteed time, the founder-only launch function enforces both gates; the existing in-database lifecycle launches at the guaranteed time regardless. The venue popup presents this as Setup → Open → Monitor → Launch, refreshes aggregate conditions every ten seconds, explains each unmet gate, and exposes only the lifecycle action relevant to the current state. *Why:* density and women-first room composition should inform an early launch without making founders calculate them or allowing a compromised client to bypass them, while the advertised guaranteed time remains a hard promise to guests.*

- **The existing-venue popup defaults to a single launch verdict rather than exposing the readiness calculation.** It shows only entry opening, guaranteed launch, people versus threshold, a plain “Acceptable” or “Needs improvement” balance status, and one answer: ready to launch, waiting for balance, waiting for people, or live. Detailed schedule and balance configuration remain available behind `Edit setup`, while create-venue still opens directly into setup. Database readiness enforcement and guaranteed launch behavior do not change. *Why:* the founder needs a decision in seconds during a live night; progressive disclosure preserves configuration without making the normal operational view feel like an analytics or settings dashboard.*

- **The founder navigation follows the night’s operating sequence: Venues → Stats → Moderation, labeled Prepare → Monitor → Intervene.** Venues is the default destination after sign-in, and the workspace navigation plus each page kicker exposes its numbered operational phase. The underlying destinations and their functionality remain unchanged. *Why:* founders begin by configuring the night, then watch live activity, and only enter moderation when an issue requires action; ordering tools around that real workflow makes the control center self-explanatory instead of presenting three equal feature buckets.*

- **The founder dashboard uses a dedicated “Soft Precision” control-center system across Stats, Moderation, Venues, authentication, tables, and portal dialogs.** A subtly layered `#F3F4F6` canvas supports white operational surfaces, `#E5E7EB` hairlines, softened gray typography, and indigo `#4F46E5` interaction accents; green/amber/red remain semantic, while the gender mix intentionally retains coral/sky/violet category accents for quick comparison. A unified top workspace bar, segmented icon navigation, tighter surface geometry, calmer table rows, inset form groups, and consistent modal action regions make this a structural visual redesign rather than a palette swap. Workflows, content, and data behavior remain unchanged, and public-product components retain the Amourette identity. *Why:* founders use these tools for operational scanning over longer sessions, so hierarchy, restrained elevation, and predictable component composition matter more than extending the consumer nightlife atmosphere into the control center.*

- **A venue is marked Trending from live eligible attendance plus arrival momentum, with at most two venues receiving the label.** The founder-only activity function scores each non-terminal venue night as its current eligible participants plus twice the number who arrived during the last 15 minutes. The Stats selector orders every venue by that score and labels only the top two that have at least two active participants and a score of four; permanent test venues follow the same calculation so founders can verify the complete experience with the populated QA fixtures. Inactive and historical venues remain available below them. *Why:* attendance shows where the crowd is, while recent arrivals distinguish a venue actively gaining momentum from one that is merely occupied. A minimum and a two-label cap keep “Trending” meaningful instead of decorating every nonempty venue, and identical QA behavior makes the feature testable before live launches.*

- **A permanent venue owns a list of upcoming scheduled nights; founders never duplicate a venue to represent another event date.** The combined Venues modal opens on a scan-friendly schedule list, keeps add, edit, and delete actions at the individual-night level, and summarizes the number of upcoming nights in the main venue list. Deleting a future unopened night uses the audited terminal cancellation path, removing it from upcoming schedules without deleting its venue or sibling nights. Only future, never-opened schedules are editable or deletable; active or previously opened nights remain viewable with their existing operational controls. The existing database exclusion continues to reject overlapping schedules for the same venue, with an earlier explanation in the UI. *Why:* venue identity, QR, city, and timezone are durable, while each event window is a separate lifecycle record; reflecting that relationship directly removes duplicate venue setup and makes recurring event planning understandable in one glance without discarding operational history.*

- **Each venue has one permanent QR code across all of its scheduled nights.** The venue modal groups the permanent link and QR above the night list with only view, copy, and download actions; the QR continues to target `/v/[venueSlug]`, where the server-side lifecycle resolves the applicable night. Individual nights do not receive separate QR codes. *Why:* venues print and reuse a physical entrance asset, while schedules change frequently; stable venue identity prevents stale Monday/Tuesday codes, reduces staff mistakes, and keeps event creation independent from printing logistics.*

## 2026-07-27

- **Founders may permanently delete a real venue from its configuration modal after typing its exact name; permanent QA fixtures cannot be deleted.** Deletion cascades through the venue’s nights, including an active night, so the confirmation explicitly states that active users are checked out and ephemeral interactions are removed. The operation remains founder-only in the database. *Why:* venue lifecycle management needs a genuine removal path for mistakes and closed partnerships, but its blast radius is large enough that a casual button or generic confirmation is insufficient; exact-name confirmation makes intent explicit while protecting the shared test fixtures.*

- **The combined Venues admin is a scan-first list with one centered create/edit configuration modal; it supersedes the same-day expanded operations/setup composition.** The list shows only venue, current night status, entry-open instant, and participant threshold progress. Clicking a venue opens its name, essential night timeline (entry opens, guaranteed launch, close), and threshold; only the lifecycle action valid for the current state is exposed. `+ Create venue` opens the same modal and atomically creates the permanent venue plus its first scheduled night. City/timezone remain required creation context because absolute venue-local scheduling cannot safely infer them; guaranteed launch and close remain visible because they govern the waiting promise and ephemeral-night boundary. Slugs are derived from the name and stay internal. *Why:* founders should scan, select, configure, and save without confronting two full tools or partial setup, while the modal still preserves the lifecycle facts the product depends on.*

- **Venue setup and venue-night operations share one admin `Venues` destination, while remaining separate database concepts.** The dedicated `Nights` tab is removed. The combined workspace leads with scheduling and live-night controls; permanent venue concerns—QR/link, profile-preview testing override, and venue creation—sit behind one explicit `Venue setup & QR` switch so they do not repeat or compete with tonight’s work. *Why:* founders think “what is happening at this venue?” rather than choosing between two overlapping admin nouns, but one venue still owns many durable night records and that data-model distinction remains correct.*

- **Moderation ordering is automatic and explainable: active work appears in `Needs Attention`, handled work moves to `Recently Handled`.** Manual status and sort controls are removed. A founder-only database RPC gives every report a deterministic score from reason severity, unique reporters, total reports, a 30-minute report burst, and recency; the UI displays the dominant plain-language reason (for example, “Underage concern,” “Reported by 4 people,” or “New report”) rather than exposing a mysterious number. Individual review moves that report down; a case-level suspension/removal moves all reports for the person/night down. Expired temporary suspensions remain in handled history as “Suspension ended,” and a new report against a reviewed case returns as active through the existing case-reopen rule. Conversation evidence does not reduce priority because requiring engagement before safety reporting would violate women-first. *Why:* founders should open moderation and immediately encounter the next best action without operating table controls, but deterministic inputs and human-readable reasons keep prioritization auditable rather than turning safety decisions over to an opaque model.*

- **The moderation workspace presents one individual report per row, while restrictions remain grouped at the reported-person/venue-night case level.** This supersedes the earlier same-day case-row/direct-row-action presentation while preserving its one-table/no-tabs decision. The row shows reporter, reported person, reason, reported-user history, reporter activity for that venue night, status, venue, and privacy-preserving interaction evidence; clicking the row opens details, with no separate Review button and actions only in the drawer. Review state is stored per report, and the parent case becomes reviewed only once all its reports are reviewed. A 30-minute or full-night restriction still applies to the person/case rather than one report. Conversation validation reads only metadata—whether a match existed and how many distinct participants sent a message—and never selects or displays message bodies. Two-way conversation is the strongest evidence, while mutual match, one-sided messaging, and shared venue-night presence remain visible evidence levels rather than silently invalidating safety reports. *Why:* individual rows answer who reported whom and expose repeat-reporting behavior directly, but enforcement must not duplicate per report; metadata provides legitimacy context without invading private conversations or requiring someone to engage with an unsafe person before reporting them.*

- **Moderation uses one dynamic, priority-sorted table rather than separate queue tabs.** Every case remains visible in a single workspace with the reported person, total and unique reporter counts, primary reason, verification, recency, status, and direct actions. Priority is the default sort; founders may minimally narrow by status or sort by recency/report volume, but “Pending,” “Multiple,” “Recent,” and “Actioned” are no longer separate destinations. Review opens the existing evidence drawer only when deeper context is needed; 30-minute suspension, removal for tonight, reviewed, and restore are available directly on the row. *Why:* those categories describe properties of the same cases, not distinct workflows; separating them forced founders to reconstruct the queue manually, while one ordered table makes urgency and the next action readable in seconds.*

## 2026-07-26

- **Moderation is organized as one verified case per reported person and venue night, with shared presence as the minimum reporting eligibility.** A report is accepted only when both profiles participated in the same durable venue night; stronger evidence is snapshotted as mutual match, conversation started, or two-way conversation, without copying message content. Shared-room presence remains sufficient because forcing a match or two-way chat would prevent someone from reporting unsafe in-person conduct, impersonation, or an underage concern before engaging — directly weakening the women-first safety invariant. Each reporter may submit at most one report against the same person per night. The founder surface groups those reports into an inbox case, treats user blocks only as supporting signals, prioritizes multiple unique reporters (and underage concerns) above recent single reports, and limits actions to mark reviewed, suspend for 30 minutes, remove for the night, or restore. Admin “suspend/remove” language is deliberately distinct from a user's private “block.” *Why:* random off-night reporting must be impossible, but safety must not depend on a woman first reciprocating or chatting with someone she needs to report; grouping immutable evidence into actionable cases gives founders clarity without retaining private conversation content or turning moderation into an analytics database.*

- **The admin Stats surface is a current-night operational snapshot, not an analytics explorer.** Its primary signal is the number of people actively checked into the selected venue night, followed by the active-room gender mix, profiles completed, and conversations started during that venue night. Scan counts, manual night/date selection, trends, funnels, raw aggregate tables, attribution, metric definitions, and repeated historical comparisons are removed from this surface. Gender counts use the same active-presence cohort as the room total and remain hidden below ten active participants. The count includes only active presence backed by a completed profile and 18+ confirmation; an increase in that eligible count produces a short, non-blocking `🎉 +N` founder notification, never on initial load or venue switching. The Stats tab deliberately has its own founder-only palette: a continuous muted-ink page ground makes cheerful pastel data cards stand forward without nesting the dashboard inside an outer card. Venue switching is direct and ordered by active eligible attendance; the highest-attendance real venue receives a `Trending` badge only while its count is above zero, and test venues are never eligible. *Why:* founders need to understand tonight's room in seconds, not interpret an analytics workbench; keeping only attendance, mix, onboarding completion, and conversation activity makes the page actionable while the cohort threshold avoids exposing sensitive composition in a small room. The arrival cue and live venue ranking make activity legible without analysis, while the separate admin palette prioritizes prolonged readability over public-brand atmosphere.

- **Venue-night configuration is editable only until waiting first opens, and every configuration write is audited.** The dedicated admin **Nights** surface schedules and operates one-off nights globally; venue setup no longer mirrors lifecycle as a misleading Live/Dark switch. A founder may edit absolute schedule instants and the threshold only while the night is non-terminal, still closed, and has never opened. Creation, migration import, and updates record before/after JSON plus the authenticated actor; lifecycle transitions remain in their existing audit and the UI combines both without exposing founder emails. Local `datetime-local` values are resolved in the venue's IANA timezone and DST gaps and repeated wall times are rejected instead of guessed. Temporary close and terminal cancel use explicit consequence confirmations: close preserves interactions and requires fresh check-ins after reopen, while cancel purges ephemeral night data. Permanent QA fixtures are isolated from normal schedule/edit/cancel controls and retain only guarded pause/reopen operations. *Why:* once guests can enter, changing the advertised operating window creates unsafe and confusing expectations; full configuration history makes founder operations accountable, rejecting DST ambiguity prevents silently scheduling the wrong instant, and separating reversible pause from destructive cancellation makes the operational risk visible at the decision point.

- **Waiting-room Realtime uses a one-row participant-safe projection, never subscriptions to other participants' presence.** `venue_night_public_state` carries only the night id, venue id, lifecycle status, aggregate eligible count, threshold, advertised deadline/close, terminal reason, and update time. Database triggers refresh it on lifecycle changes and attendance arrival/departure; authenticated clients may select a row only after they have participated in that exact night. The participant page subscribes to this projection for immediate count and launch/pause/terminal transitions, polls it every 30 seconds, and resyncs on foreground return. The existing narrow `venue_night_state()` RPC remains the pre-check-in entry contract and is resolved before onboarding, so a closed venue never asks someone to build a profile. Once checked in, the exact night id is held in per-tab `sessionStorage`; a refresh can therefore reload the RLS-safe projection and preserve pause/cancellation/end instead of collapsing them into generic off-hours. Manual close renders a recoverable pause and keeps watching; cancellation and scheduled end render terminal exits. The permanent `/v/test-waiting` fixture uses the ordinary engine with a past waiting-open, year-9999 deadline/close, and an unreachable integer threshold, rather than a production-code bypass. *Why:* waiting participants need shared count and lifecycle signals, but exposing presence rows to Realtime would leak identities before double opt-in. A tiny aggregate projection gives Realtime a durable row with RLS, lets terminal events remain readable after presence is ended, and keeps polling and foreground recovery on the same authoritative facts.

## 2026-07-26

- **Do not adopt a copied cookie-backed anonymous Supabase session as the Safari → PWA identity bridge.** Physical testing for #119 proved that installation can copy the same UID and `session_id`, and ordinary Safari/PWA alternation can refresh successfully, but the deliberately stale-context case failed: after Safari rotated twice, the PWA received `Invalid Refresh Token: Already Used`. The unchanged UID immediately afterward was temporary evidence from an access token that still had almost an hour to live, not proof that renewal remained healthy. Supabase's rotating-token security model only tolerates the reuse window and the active token's direct parent; two independently evolving cookie jars can therefore strand or revoke the shared session. Transient Realtime `CHANNEL_ERROR` states recovered to `SUBSCRIBED` and were not the deciding failure. #120 must be reframed around an explicit identity link/upgrade or a PWA-owned post-install session, and #121–#122 remain blocked until that bridge is proven; weakening refresh-token reuse detection is not an acceptable workaround. *Why:* the product cannot promise persistent identity if a normal Safari/PWA usage pattern can remain apparently healthy until the current JWT expires and then lose renewal, and disabling a theft-detection control to preserve the handoff would trade continuity for security.*

- **Language switcher (#42) redesigned to a flag pill dropdown, and the switcher's home is narrowed to entry + settings + menu escape-hatches, off transient shells.** The old switcher was a native `<select>` styled as a champagne pill showing text codes (EN/FR/ES), rendered in nine places. Explored four directions on a throwaway `app/language-lab/` route (deleted before merge, same convention as `room-chrome-lab`/`chat-preview`): A pill dropdown, B flag-only trigger, C always-visible segmented flags, D mobile bottom sheet. The calls that closed: (1) **variant A** — the familiar pill, now a real keyboard-navigable Radix `DropdownMenu.RadioGroup` (arrow keys, Esc, typeahead, focus, checked indicator) opening a bordeaux panel that fades+scales from the trigger corner (`.lang-menu` keyframes in `globals.css`, `prefers-reduced-motion` respected); B/C/D kept only as reference. (2) **Inline SVG flags, not emoji** — emoji flags fall back to letter boxes on Windows/Chrome, unacceptable on a public landing; the flags are hand-rolled SVGs rendered as whole rounded rectangles, never circle-cropped (a circular crop eats the US canton, which sits in a corner). (3) **US flag for English, clean solid canton** — English shows the US flag (NY-first launch with Aymane) rather than the UK convention or a muddy split UK/US roundel; at the ~14-16px the switcher renders, individual stars alias into blobs, so the canton is drawn solid (stars were confirmed illegible on-device via the lab's size ladder — they only render cleanly at ≥22px, which the switcher never uses). (4) **Placement doctrine** — the locale is a one-time entry choice persisted in `localStorage`, so the switcher belongs at entry (landing, onboarding step 0), in settings-like surfaces (profile, profile editor), and as a discreet escape-hatch inside the room/chat menus; the three floating-pill placements on *transient* shells (the `invisible` state, and the loading/error shells in room and chat) were removed as noise. Dead `LANGUAGE_OPTIONS` dropped from `lib/useLocale.ts`; no i18n/state/logic change, purely presentational. *Why:* the switcher looked unfinished next to the rest of the v2 polish, and "flag + polish" is only worth it if the flags actually render everywhere (hence inline SVG over emoji) and read at switcher size (hence US canton over aliased stars); narrowing placement follows the existing onboarding-step-0 rationale that language is chosen once at the door, not re-picked on a loading screen. One thing to eyeball in preview: the nested Radix dropdown inside the room/chat popover menus (`v:1541`, `chat:582`), which can't be exercised without a live night.

## 2026-07-28

- **Admin password recovery and rotation stay inside the existing Supabase Auth + founder allow-list boundary.** A generic `/admin` recovery request sends Supabase's email link to `/admin/reset-password`; only Supabase's one-shot `PASSWORD_RECOVERY` redirect event unlocks that route, and the recovered session must still pass `am_i_admin()` before the reset form renders. Founder status alone is insufficient, so an ordinary `INITIAL_SESSION` or `SIGNED_IN` admin session cannot bypass current-password confirmation by navigating directly to the reset route. An authenticated founder rotates a known password only after re-entering it, and both successful paths sign out all sessions. No custom reset-token table, service-role endpoint, database migration, or broader cookie/SSR auth conversion is introduced. *Why:* Supabase already owns password hashing, expiring recovery tokens, session issuance, and password policy, while the event proves the session came from the recovery ceremony and the locked `admins` allow-list supplies the separate authorization check. Reusing those primitives is smaller and safer than handling credentials ourselves, and global sign-out gives a recovered or rotated credential a clean session boundary.*

- **Email unsubscribe is global by normalized address, while re-subscription remains owner-scoped (#64).** Public email links carry a random 32-byte opaque token whose SHA-256 hash, address scope, 12-month expiry, and optional revocation are stored only in the private schema. A GET validates without mutation or address disclosure; an explicit POST suppresses every active anonymous identity using that address and is idempotent. A recognized device may also unsubscribe globally without submitting an address, while an explicit re-subscription updates only that device's owner row with fresh consent. Any still-valid older token can suppress the address again after re-subscription. *Why:* delivery is deduplicated by address, so withdrawal must have the same scope; confirmation prevents mail-security scanners from withdrawing consent, hashed scoped tokens minimize breach impact, and retaining suppression state prevents duplicate identities from silently opting someone back in.*

- **`email_subscriptions` is the sole marketing-consent store, owned directly by the anonymous Auth user and independent of profile/account lifecycle (#105).** One owner-scoped row records the normalized address, locale, acquisition source, consent version, explicit subscribed/unsubscribed state, and timestamps. Landing, room popup, and the future waiting room share the same idempotent persistence path; browser storage records only a venue-night dismissal and is never subscription authority. Existing opted-in `profile_private` consent is migrated with a deterministic temporary `en` locale, then its email/consent columns are dropped while age/phone remain. Global-address unsubscribe is still #64 and verification/delivery/deduplication is still #63. *Why:* marketing consent is global and can precede profile creation, so tying it to `profile_private` created two stores and made room/browser state authoritative. A single Auth-owned row preserves the zero-friction anonymous journey while making consent state enforceable by RLS and reusable by every capture surface.*

- **Defer installable PWA and Web Push beyond V1; keep anonymous QR entry plus Realtime, and offer optional email only for future-night announcements.** A recoverable identity link could technically preserve the same Supabase UID while giving Safari and an installed PWA independent sessions, but the resulting journey asks someone in a live bar to understand Add to Home Screen, install the web app, and complete an email or OAuth identity step before receiving the benefit. That is too much friction at the product's most time-sensitive threshold. #113 and #120–#122 are therefore closed rather than reimplemented around an account conversion. The waiting room will instead expose a secondary optional email action through #136, reusing the global consent delivered by #33, suppressing duplicate prompts, and keeping marketing email strictly separate from authentication; ignoring it leaves the complete anonymous Realtime flow unchanged. The identity problem is not declared solved or discarded: a board question retains the future choice between converting the anonymous Supabase user, performing a one-time ownership transfer, or introducing a stable application-level identity when native-app planning begins. *Why:* optimizing a future background notification must not weaken the immediate scan-to-value experience, and a PWA-specific identity layer would add security and operating cost before the core live-night hypothesis is proven. A native app will still require an explicit, takeover-resistant web-to-native identity design, but familiar installation, Universal Links, secure storage, system authentication, and native Push give that future decision a materially better user context.*

- **Pause discovery and leaving a venue are distinct controls.** Pause discovery keeps the participant checked in and included in attendance, hides their profile, and pauses browsing while preserving access to existing conversations. Leaving is isolated as a confirmed venue-level action: it closes active presence and removes the participant from discovery and attendance, but does not delete the night's likes, matches, or messages. *Why:* someone may need control over exposure without claiming they physically left; conflating those intentions makes the live count dishonest and turns a reversible safety control into a disruptive checkout.
- **Presence history, not browser storage, gates re-entry; heartbeats may only touch an active row.** Before calling `check_in()`, the room reads the participant's presence rows for the exact `venue_night_id`: an active row resumes, no history auto-checks in as a first arrival, and ended history renders checked out until a one-use explicit re-entry intent authorizes a new presence period. The heartbeat now updates `last_seen_at` on that active row instead of invoking `check_in()`. This supersedes the 2026-07-28 entry-threshold decision's instruction to replay the arrival ceremony after leaving: confirmed re-entry is quiet. *Why:* database history gives refreshes, old links, new tabs, and back/forward navigation one consistent answer, while a delayed heartbeat can never undo an intentional departure.

- **A return to a previously left venue is presented as a welcome-back threshold, while re-entry remains explicit.** The primary action rejoins the venue and the secondary action returns to the entrance; similarly, a departure confirmation makes staying the primary safe action and leaving secondary. Joining another venue still atomically checks the participant out of the previous one, preserving the one-live-venue invariant. *Why:* presence history must prevent an old URL from silently checking someone back in, but neutral internal checkout state should not read like a reprimand or make the more disruptive action visually dominant.

- **Venue departure preserves matched conversation history but pauses new messages until both participants are physically present again.** Immediately after checkout, the departing participant sees a departure confirmation; only a later return to the venue URL becomes a welcome-back threshold. Inside chat, the departed participant's status changes from present to left, the existing thread remains readable, and the composer is replaced with an explanation until both profiles have active presence in the match's exact venue-night. Discovery pause does not pause chat. The database enforces the same rule as the client. *Why:* preserving the night's shared context avoids making departure destructive, while allowing remote messaging after someone leaves would turn a live, in-person trigger into an open-ended chat product and make the “in the room” signal dishonest.*

- **The waiting-room email action takes priority over the timed live-room email popup for that venue night (#136).** Presenting the optional inline action records a dedicated `waiting-room-offered` browser marker immediately, even if the guest never expands it, while “Not now” additionally records the existing nightly dismissal marker and hides the action. Active subscriptions remain authoritative in `email_subscriptions`; an unsubscribed owner sees the stored address prefilled, and submitting uses the shared `waiting_room` acquisition source without changing Auth identity. Check-in, lifecycle Realtime, and automatic room opening do not depend on this form or either marker. *Why:* two email asks in one night feel coercive, while making an optional marketing form part of entry or launch would weaken the zero-friction live journey. Separating “offered” from explicit dismissal preserves the distinction without letting the later popup compete with the waiting-room surface.*

- **“Not now” collapses the waiting-room email form instead of removing its CTA; successful signup replaces it with a compact green confirmation state (#136 clarification).** The nightly dismissal marker still suppresses the later live-room popup, but a guest who remains in the waiting room may reopen the inline form and change their mind. Confirmation does not leave a persistent paragraph competing with the waiting-room content, and its copy uses neither emoji nor an em dash. *Why:* dismissal should reduce pressure without making a reversible option disappear, while success needs a quiet, durable acknowledgement rather than another block of celebratory copy.*

- **Repeatable QA is fixture-first, preview-origin-aware, and guarded on the shared development database.** `/qa` verifies three permanent fixtures before acting: crowded and empty remain live, waiting remains waiting with an unreachable threshold, and every night closes in year 9999. It confirms a ready deployment through GitHub, uses Vercel's stable branch alias when available (including querying Vercel for long hash-suffixed aliases), suggests a fixture from the branch diff while honoring an explicit override, and identifies a branch-preview tester by observing exactly one new non-synthetic Supabase check-in. Match preparation chooses an already-compatible synthetic profile, resets only that fixture/tester pair, and preserves the real reciprocal-like action; it never reads `profile_private` or widens the service role's deliberate PII boundary. Incoming message and presence simulations require explicit shared-write flags. Full fixture reset is separate, destructive, explicitly confirmed, and verified afterward. The same repo CLI backs matching Claude Code and Codex skills, neither of which may ship, merge, or apply migrations. *Why:* different branch preview origins create different anonymous UUIDs, while deployment-specific URLs would unnecessarily create another identity after every push and the single Supabase development database is shared across Paris and New York; a stable branch origin plus observation and strict ambiguity failures makes short-lived previews convenient without guessing identities, silently destroying the other founder's test state, or weakening least-privilege grants for QA convenience.

## 2026-07-29

- **The chat shell is pinned to `visualViewport` with a fixed, translated `<main>`, and the thread owns the reading position (#100).** The shell is `position: fixed` at the layout origin, sized to `visualViewport.height` and offset by `translateY(visualViewport.offsetTop)`, with both values written to CSS variables inside a single coalesced `requestAnimationFrame` and never transitioned. `html` and `body` are locked (`overflow: hidden`, `overscroll-behavior: none`) while the page is mounted, so the document cannot scroll and only the thread does. The transform is kept even at `0px` on purpose: it makes `<main>` the containing block for its own `position: fixed` descendants, which is what pins the ambient gradient layers and the report/block panels to the visible viewport rather than the layout one. The composer's bottom clearance is now `env(safe-area-inset-bottom)` plus a small pad, dropping the previous hardcoded `2.5rem` lift, and the safe-area term is omitted entirely while the keyboard is open. The message field moves to a 16px font size, below which iOS Safari zooms the page on focus. Auto-scrolling becomes conditional: the thread pins to the latest only when the reader is already within 80px of the bottom or the new message is their own, a `ResizeObserver` preserves the distance from the bottom across keyboard, chrome, and rotation changes, and a WhatsApp-style jump-to-latest control with an unseen count is the only way the thread moves under the reader. The outside-tap closer for the overflow menu now ignores `[data-radix-popper-content-wrapper]`, because the nested language dropdown renders in a portal and was closing the menu mid-gesture. *Why:* `dvh`, `svh`, and `interactive-widget` do not solve iOS Safari, whose floating bottom bar overlays CSS-viewport content without shrinking any height unit and whose visual viewport can shift while the layout viewport stays put; only measuring and following the visual viewport keeps the composer reachable. Height-only measurement, which is what the page did before, left the shell drifting whenever iOS moved the visual viewport. Making auto-scroll unconditional was equally wrong in the other direction: it took the reading position away from the person reading history, which is precisely what the jump control gives back without a single silent jump.

- **`getamourette.com` is registered with Cloudflare, remains on Cloudflare DNS, and serves the renamed Vercel project `amourette-webapp` in Production.** The apex domain is the canonical production URL and `www` redirects to it; branch previews stay on `amourette-webapp-git-<branch-slug>-tothe-moon.vercel.app`. The domain was purchased through Marwane's existing Cloudflare account for speed, with shared company ownership and account structure to be revisited later; email providers remain independently configurable through standard DNS records. *Why:* keeping registration and DNS outside Aymane's personal Vercel account reduces hosting lock-in and preserves freedom to move the web or email infrastructure, while using the existing Cloudflare account avoids delaying the launch configuration.

- **Venue identity and venue-night scheduling are separate founder workflows, superseding the atomic combined-configuration decision from 2026-07-27.** A founder first creates or independently updates a permanent venue's name and controlled rollout location, then adds, edits, or operates nights in live/active, upcoming, and historical groups. The initial rollout exposes only the coherent Paris/Europe-Paris and New York/America-New-York pairs. Guaranteed launch must remain later than entry on the same local date; only closing may explicitly roll into the next day. *Why:* permanent place metadata changes independently from ephemeral sessions, loose city/timezone pairs create scheduling ambiguity, and silently rolling launch into tomorrow can create an unintended 23-hour wait.*

- **Admin Stats selects exactly one venue night by lifecycle priority and binds every metric to that night.** Selection is live, then waiting, then the nearest upcoming night, then the latest historical night. The operational outcomes are active participants, aggregate likes per active participant, matches, and conversations, with profile completion shown only when a real aggregate row exists. *Why:* mixing a future schedule with the latest historical analytics produces a plausible but false dashboard, while substituting attendance for missing test analytics fabricates product performance.*

- **Thirty-minute moderation suspension is deferred, superseding the temporary-suspension decisions from 2026-07-26 and 2026-07-27.** Report collection, privacy-preserving evidence, automatic prioritization, per-report review, full-night removal, and restoration remain. The action is absent from the founder UI and rejected by the database function; historical suspended rows remain readable and restorable. *Why:* a temporary restriction policy and dedicated participant-facing restricted state have not been agreed, so silently ending presence would leave a participant on a stale room with an unexplained failure.*

- **Founder QR tools always encode the canonical production venue URL.** A branch preview displays and downloads `https://getamourette.com/v/[slug]`, labels it as the production QR, and never substitutes the preview origin. *Why:* a physical QR is a permanent venue asset, while preview hostnames are branch-scoped and disposable.*

## 2026-07-30

- **A room profile can be liked by double-tapping the photo, while the visible heart remains the explicit and accessible control (#37).** One tap still expands or collapses the bio after a 280ms gesture-disambiguation window; two taps within that window add a like and acknowledge it with a restrained fading heart over the photo. The gesture is additive only: double-tapping an already-liked profile does nothing, while pressing the filled heart remains the deliberate way to undo a private like. Interactive controls are excluded from card-tap detection, repeated submissions are guarded by the existing pending state, and reduced-motion users receive the state change without the overlay animation. *Why:* double-tap makes the core action natural on a phone without hiding it behind an undiscoverable gesture, and reserving unlike for the explicit control prevents an enthusiastic or accidental repeat gesture from silently reversing intent.*

- **Double-tap feedback follows the familiar TikTok spatial pattern, translated into Amourette's visual system (#37 refinement).** A clean solid-red SVG heart blooms at the exact second-tap position, holds briefly, and fades upward over 900ms; repeated double-taps may overlap visual hearts but never send duplicate likes. This supersedes the first fixed-centre feedback treatment, while preserving the explicit filled-heart unlike control and reduced-motion behavior. *Why:* touch-local feedback makes the gesture feel causally connected and familiar, while a platform-independent SVG and Amourette palette avoid the inconsistent system-font heart seen in physical-phone QA; the tested cream outline was removed because it made the heart read as a sticker rather than a warm in-photo event.*

## 2026-08-02

- **The #63 email rollout separates an additive delivery foundation from revoking direct subscription writes.** The outbox tables, server-only RPCs, worker dispatch, and inert-without-Vault cron ship first while the existing authenticated RLS write policies remain available to already-loaded clients. Only after the exact application commit is live and the production endpoints, Vault dispatch, and Resend lifecycle have passed end-to-end checks does a second migration remove the three write policies and revoke `INSERT`, `UPDATE`, and `DELETE` from `authenticated`. *Why:* the shared pre-launch database has no branch environment, and separating the behavioral cutover preserves a working legacy path if any infrastructure or delivery check fails before revocation instead of turning a reversible rollout problem into an outage.*

- **Email reputation events are terminal and outrank provider timestamps.** A hard bounce, complaint, or provider suppression always suppresses the delivery and normalized address, even if Resend gives a later `email.delivered` event a slightly newer timestamp; later delivery events cannot restore a suppressed delivery. Provider event time remains monotonic for audit ordering. *Why:* production E2E showed Resend timestamping `email.delivered` 231 ms after `email.complained`, so timestamp-only last-write-wins silently discarded the reputation signal that protects recipients and sender reputation.*

- **The room match stack orders conversations by attention: unread first, then most recent activity, with a stable fallback.** The collapsed preview uses the same order as the expanded strip, so a match that sends a new message moves to the leftmost position and remains immediately visible. *Why:* the stack is a live path back into active conversations; leaving a newly active match behind older unordered results hides the most relevant action and makes unread badges harder to find.*

- **Chat regression coverage is hybrid: pure state tests plus sequential two-session Playwright scenarios against disposable remote Supabase fixtures.** Fast tests own deterministic ordering, delivery reconciliation, retry identity, read markers, and unread isolation; browser tests own RLS authorization, real REST/Realtime delivery, typing, presence pauses, safety loss of access, and one-versus-four-match layout. Every run creates a uniquely named non-`test-*` venue, active night, anonymous users, profiles, presence, and matches, and deletes the venue and Auth users even after failure. Permanent shared QA rooms remain reserved for the later founder-gated physical iPhone/PC pass. Android remains a separate physical-device gate and is never inferred from browser emulation. *Why:* transport and RLS regressions only appear against the real Supabase contracts, while deterministic state transitions are faster and clearer as pure tests; isolation prevents repeatable automation from corrupting either founder's shared manual QA state.*

- **Double-tap feedback is a discreet one-time state acknowledgement, not a repeatable social animation (#37 refinement).** The touch-point heart now uses the same glyph as the explicit like control at 52px, with a restrained 560ms scale-and-fade and no bright bloom. It appears only when the gesture changes an unliked profile to liked; further double-taps stay quiet, and unlike remains exclusive to the visible button. This supersedes the larger 96px overlapping-heart treatment from 2026-07-30. *Why:* physical-phone QA found the previous heart and motion too dominant for a discreet double opt-in, and replaying feedback without a state change falsely suggests that another like was sent.*

- **The final double-tap glyph is the filled typographic heart, held for 700ms (#37 clarification).** Phone comparison kept the smaller 52px button-family silhouette but restored the filled red state and gave the restrained scale-and-fade slightly more time to read. *Why:* the outline alone looked unfinished as confirmation of a completed like, while the earlier 560ms treatment disappeared a little too quickly on-device.*

- **The selected double-tap artwork is the filled Lucide heart at 52px (#37 final visual choice).** A temporary phone comparison tested the exact button-glyph composite, Lucide, a slender custom path, and a softer custom path under the same 700ms motion; the Lucide option was selected and the lab was removed before merge. This supersedes the typographic-heart artwork choice above while retaining its timing and one-time feedback behavior. *Why:* using the maintained icon shape produced the cleanest filled silhouette on-device without introducing a hand-drawn third heart into the interface.*

- **Sending a message never closes the software keyboard, and nothing about the keyboard's arrival passes through React (#100).** The send button cancels its own `pointerdown`, so the tap never moves focus out of the field, and `sendMessage` re-focuses the field synchronously before its first `await`, which covers the iOS keyboard's own "send" key (implicit form submission blurs the input). Both are required and neither is sufficient. The `keyboardOpen` React state is gone: the same coalesced `requestAnimationFrame` that writes `--app-vh` and `--app-vv-top` now also writes `--app-kb-safe` (`0` with the keyboard up, `1` otherwise), and the composer's clearance is `calc(env(safe-area-inset-bottom) * var(--app-kb-safe, 1) + 0.75rem)` in CSS. The four ambient gradient layers move into a wrapper sized to `100vh` instead of following the shell; they still sit inside the transformed `<main>`, so they still track the visible viewport, but their geometry no longer changes with the keyboard. *Why:* iOS only reopens a keyboard for a `focus()` that runs inside the user gesture, so a refocus after the Supabase insert resolves is already too late — the fix has to be synchronous and has to cover both send paths. The rest is about removing our own jank rather than chasing native parity: a state flip re-rendered the whole thread on the exact frames the keyboard animation needs cheap, and percentage-defined full-screen gradients tied to a changing height re-rasterised five layers per crank and visibly breathed while doing it. The residual stepping is not ours to remove: a native app gets `keyboardWillShow` with the exact duration and curve and animates in lockstep, whereas `visualViewport.resize` only notifies after the fact, in coarse increments. Masking those steps with a CSS transition on the shell is explicitly rejected — it is lag, which is precisely what #100 forbids.

- **The chat shell shrinks itself on field focus, ahead of the keyboard, so iOS never pans the page (#100).** On `focus` of the message field the shell is immediately resized to `innerHeight - <last measured keyboard height>`, the measurement being persisted in `localStorage` keyed by viewport size (an estimate of 42% of the viewport covers only the very first focus ever made on a device). While that prediction is pending the measured height may only shrink the shell further, never grow it back, and the prediction is abandoned as soon as a real keyboard height is measured, on blur, or after a 600ms fallback for touch devices with a paired hardware keyboard. **The remembered height only ever grows**, and carries 12px of clearance: the closing animation walks the inset back down through every intermediate value, so the last measurement still above the detection threshold sits near the threshold itself, and persisting it recorded a ~130px keyboard that predicted almost no shrink at all — which is why the first focus of a session behaved correctly and every one after it regressed to the original defect. *Why:* iOS decides whether to pan the visual viewport to reveal a focused field from where that field sits **at the moment of focus**, and it only announces the pan through `visualViewport` once its own animation is already running. That late notice was the entire defect Marwane saw on the phone: for the length of the keyboard animation the page was panned and our compensating `translateY(offsetTop)` was not applied yet, so the composer flew to the top of the screen and the header slid out above it, then everything snapped back when the events finally landed. It cannot be fixed by reacting faster, because the notification itself is late by construction — the only move is to make the field already sit above the keyboard line before iOS gets to choose, which removes the reason to pan at all. This is also why the earlier work on render cost and gradient repaints did not help: the symptom was never a frame-rate problem.

- **The chat shell is anchored to the bottom of the layout viewport and never reads `visualViewport.offsetTop`, with a runtime regime check for browsers that do not pan (#100).** An on-device trace from an iPhone established three things that had each been assumed the other way round: `window.innerHeight` collapses along with the keyboard (714 → 377), so every `innerHeight - visualViewport.height` keyboard test read 0 and never fired; `documentElement.clientHeight` stays at the full height throughout and is the only sound reference; and `visualViewport.offsetTop` reports 0 for a flat 400ms after the page has visibly panned, on every single opening, still 0 when read every animation frame — the property is stale, not the event late. Since iOS pans exactly so that the bottom of the layout viewport meets the top of the keyboard, a shell anchored to `bottom: 0` and sized to the measured visible height lands correctly in every observed state without consulting the offset at all. Because a browser that shrinks the visual viewport *without* panning would leave that same shell underneath the keyboard, the effect settles the regime by observation on the first keyboard opening (600ms after first sight of it, is the offset still zero?) and lifts the shell by the keyboard height when it is. `translateZ(0)` became `translateY(var(--app-shell-shift))`, still load-bearing as the containing block for the fixed descendants. *Why:* three successive fixes were built on plausible but wrong models of what iOS does — that it pans to reveal the focused field (it pans regardless), that pre-empting the pan by shrinking the shell on focus would prevent it (it does not), that the lateness was in the event delivery (it is in the property). Guessing cost four rounds of physical testing; one trace shipped to the deployment's runtime logs settled it in one. Prefer instrumenting the real device over reasoning about the platform. The regime check exists because Android Chrome is genuinely unverified and the failure mode there is not cosmetic but a chat that is not on screen at all; it converts that into a wrong first 600ms, which is a rough edge rather than a blocker.

- **The typing indicator lingers like WhatsApp's and re-pins the thread when it appears (#100).** The sender keeps claiming "typing" for 3s after its last keystroke (was 1.6s) and the receiver holds the bubble a further 1.5s past the last broadcast (was 0.7s) purely as a safety net for a lost "stopped". The bubble is rendered inside the scrollable thread, so its arrival grows the content without firing a scroll event and without resizing the thread box the `ResizeObserver` watches; an effect on the typing flag re-pins the thread, and only for a reader already at the latest. *Why:* at 1.6s the bubble blinked off during any pause to think mid-sentence, which reads as the other person leaving rather than composing. And with nothing else moving the thread, the bubble was born below the fold on a long conversation: it existed but no one saw it. The re-pin keeps the rule that the reading position belongs to the reader — a reader scrolled back into the history is not moved, their distance from the bottom is merely refreshed.

- **Complete match chat in ordered layers: mobile shell, reliable text delivery, exhaustive regression coverage, first-contact aids, then private photos.** The mobile viewport/composer is the first blocker (#100), optimistic exactly-once text delivery and recovery follow (#96), and automated plus physical two-device validation remains its own visible gate (#90). The landing availability bug stays separate (#144); contextual first-message suggestions and a lightweight matched-profile preview form one later friction-reduction unit (#154); private ephemeral photo messages form a distinct schema/Storage/safety unit (#155). The old mobile overflow-menu bug (#29) is absorbed into #100 rather than maintained as a parallel ticket. *Why:* the previous #96 mixed transport reliability, mobile layout, product exploration, profile access, QA, and media security in one unfinishable issue. The sequence makes every unit independently reviewable and prevents attractive additions from landing on an unreliable messaging surface.*

- **Chat exposes technical recovery states, not social surveillance states.** Messages may show transient pending and actionable failed/retry states, with successful server persistence as the confirmation boundary; V1 does not add persistent sent checkmarks, device-level delivery receipts, or read/seen receipts. The existing typing indicator remains lightweight, ephemeral, and non-persistent. *Why:* people need to know whether their own action failed, but delivery/read telemetry adds social pressure and interpretation without helping the first in-person hello, conflicting with the calm women-first experience.*

- **Chat motion communicates state calmly and never fabricates delivery.** Viewport, composer, menu, optimistic message, failure, retry, suggestion, profile-preview, upload, and photo-reveal transitions must remain stable, restrained, accessible, and equivalent under `prefers-reduced-motion`. Reconciliation must not replay message entrance, progress must not shift the thread, and animation must never make an unconfirmed message look delivered. *Why:* fluidity is part of perceived reliability on a phone, but decorative or misleading movement erodes trust precisely where Realtime and network state are uncertain.*

- **V1 excludes message reactions and read receipts; photo messages are private, ephemeral, and protected before reveal.** Reactions, video, audio, GIFs, documents, galleries, editing, forwarding, threaded replies, and post-night retrieval are outside the completion scope. A photo message may carry one image, uses private matched-participant authorization, strips unnecessary metadata where practical, expires with the conversation, retains direct report/block access, and is initially tap-to-reveal for the recipient. *Why:* reactions and broad messaging features optimize time inside the app, while unsolicited media creates a distinct women-first safety risk. A deliberately narrow protected photo path can help two people locate or contextualize each other without turning the product into a durable social messenger.*

- **V1 marketing email uses explicit single opt-in rather than mandatory double opt-in.** Submitting the clearly labelled future-night announcement action immediately records consent, shows the captured address with a correction path, and sends a welcome message without requiring a confirmation click. Campaign eligibility requires valid syntax, recorded consent, and no known operational suppression; permanent bounces and complaints suppress the normalized address immediately, every marketing message includes global unsubscribe, and rate limits protect the capture endpoint. This replaces #63's earlier assumption that “verified” necessarily meant mailbox-ownership confirmation; double opt-in remains available if observed address quality, complaints, scale, or legal guidance justify it later. *Why:* asking a guest to leave the live bar experience, open their inbox, and return would disproportionately lose intentional subscriptions at Paramour's most time-sensitive moment. At V1 volume, explicit and auditable consent plus a welcome message, easy correction, suppression handling, and conservative send frequency provide a better balance of conversion, compliance evidence, and sender reputation.*

- **Resend is a transport only; Supabase owns consent, suppression, and the durable provider-neutral email outbox.** Server-authenticated subscription writes atomically enqueue a repository-rendered welcome email, while Resend receives pre-rendered HTML/text through its Email API and returns delivery events through a signed, idempotent webhook. A `pg_cron` job calls the authenticated application worker through `pg_net` with credentials held in Supabase Vault; each delivery is claimed atomically only after consent and suppression are rechecked. Contacts, Broadcasts, dashboard templates, open tracking, and click tracking are not used. Definite transient HTTP failures are retried after backoff; network timeouts and interrupted claims become `unknown` and are never resent without verification. Campaign authoring and frequency policy live in a separate admin task (#158), blocked on the admin redesign (#149). *Why:* one canonical store prevents consent drift and keeps raw addresses behind RLS, a generic outbox gives later campaigns the same safety semantics, code-owned multilingual templates remain reviewable, and treating ambiguous sends conservatively avoids duplicate mail and reputation damage.*
- **An empty live feed is three framings and one action set, and it never explains itself (#118, #152, #147).** A feed can be empty because nobody has arrived, because everyone present is outside the participant's preferences, because they are all already matches, because of a block in either direction, or because the only people here are checked in invisible. The last three are indistinguishable client-side (RLS strips those rows before the client sees them) and none may ever be named on screen. So the empty room varies only its framing: `alone` (nobody but us checked in), `emptied` (same, but the room held a crowd earlier tonight, so promising a fill-up right after watching it drain would ring false), and `live` (people are here, the feed is empty anyway, and the copy says only that people come and go, plus "the moment someone turns up *for you*" as the honest hedge). An unreadable participant count falls back to `live`, the only variant that asserts nothing about the room. The earlier plan to point participants at their matches when they had exhausted the room was dropped: a match that already met in person and did not click makes "go talk to them" absurd, and the matches pill in the chrome already offers that door without instructing anyone. *Why:* the product's red line is that no one ever learns they were passed over; every extra word explaining an empty feed walks toward that line, and the participant's available actions are identical in all cases anyway. The variant logic lives in `lib/empty-room.ts` so it is testable without a browser (`npm run test:empty-room`); the RLS half stays covered by `npm run test:venue-nights`.

- **The browser-notification opt-in is removed from the empty live room, replaced by the next-nights email card, and the waiting room and empty room now share their action cards.** The notify card only ever requested the browser permission: nothing was wired behind it, and Web Push on iOS requires an installed PWA, the friction #119 rejected. So on iPhone in Safari it rendered nothing at all, and on Android it asked for a permission no server could use. Both screens now show the same two cards, the bio lever (adapting to an empty bio, which closes #147) and the email opt-in, in the same order, from `RoomCards.tsx`. The empty room's card gets its own `empty_room` source (widening the `email_subscriptions_source_valid` check), and both screens now show one shared title and body: the wording that was written for the empty room. *Why the extra source:* `source` answers "where did we ask" and `consent_version` answers "what did they agree to", so sharing a version (all three live-night surfaces show the same audited sentence) is right while sharing a source is not — reporting `room_popup` from an inline card would have merged the two surfaces in the data permanently, and nothing consumes the column yet, so the correction was free now and never again. Widening a check constraint is permissive: no existing row can become invalid and older code cannot emit the new value. *Why one wording:* the two screens share the card component, so two titles for the same ask is exactly the drift `RoomCards.tsx` was extracted to prevent. Showing the card suppresses the 2-minute popup for the night (the ask has been made), and a participant already subscribed sees a blush confirmation instead of being asked twice, blush and a check rather than the off-palette emerald the first pass used. *Why:* an opt-in that promises a notification nobody can send is worse than no opt-in, and the email is the only next-nights promise we can actually keep before native.

- **The waiting room's header is the live room's chrome, to the line, and the live dot belongs to the count.** Wordmark, venue name on its own line, then the red dot in front of the participant count, which moved up out of the body to get there. The "you're here" kicker was dropped (it labelled nothing the participant could not see) and so was the city: you scanned a QR code inside the place, you know what city you are in, and the live room chrome never showed it either. *Why:* the two screens are the same moment of the night, so they should be the same place; and the dot is a promise of liveness, which is true of the count and of nothing else on that screen.

- **The empty room says "you are on your own here for now", never "you are the first one tonight".** `participant_count` is an instantaneous count and the "had a crowd" flag only remembers what happened since this page loaded, so a room that filled and emptied before the scan is indistinguishable from one nobody has entered. *Why:* the `alone` framing is the one place the copy was asserting a history the client cannot see, and being caught in a small provable lie is how a stranger decides the rest of the screen is marketing.

- **Feed transitions are acknowledged in both directions, and a form being filled holds the feed back.** `feedTransition()` classifies a feed change as `arrival`, `drained`, or `none`: the first fill after load is deliberately not an arrival (that is the room loading, not something moving under the thumb), and draining to zero shows a transient neutral line on the empty room ("no one left to see for now") that is true whether the last profile left, blocked us, or became a match, and says which one it never. A match reveal suppresses that line, since the celebration is acknowledgement enough. If an answer is *being typed* when someone eligible arrives, the room does not swap under the participant: the arrival is announced as a full card and entering the feed stays their call. The hold is deliberately narrow, and an earlier, wider version of it was a bug: holding for any *open* form meant that opening the email card out of curiosity froze the empty room for good, while the framing copy still mutated underneath ("it's still early" became "the night is on") and the only way out was a pill at the top of a container the form had just scrolled away from. On a real browser the arrival never appeared, which is indistinguishable from a room that stopped updating; it was mistaken for a Realtime failure, and the Realtime path was proven healthy at the same time (a second participant's `presence` INSERT and the `venue_night_public_state` UPDATE both reach a subscribed client in ~400ms). Only a form the participant has actually touched holds the room now. *Why:* the existing rule that the feed never moves under the thumb had no equivalent for the feed vanishing entirely, which is the more jarring of the two — but protecting a form nobody is filling costs the one thing this screen exists to deliver, which is the person who just walked in.

## 2026-08-20

- **Private photo messages (#155) are deferred beyond V1.** V1 chat remains text-only; this does not affect profile photos or the profile-photo crop work in #31/#181. *Why:* photo messaging is not required to prove the live, in-person match loop and would add disproportionate moderation, unsolicited-media, storage, metadata, and access-control risk before the core product has been validated. Leading dating products also demonstrate that a useful match chat does not require in-chat photo sharing, so the added surface is not justified for launch.*

- **Paramour's first launch is a deliberately concentrated venue night, not a broad app launch or a permanent events business.** The launch uses one desirable partner venue, one advertised time window, founder-led participant recruitment, and a capacity-limited guest list to create enough simultaneous compatible attendance for the discreet match loop to work. The venue remains a normal night out: there are no public single badges, forced dating games, or open messaging, and the event format is a launch wedge to be replaced progressively by recurring venue usage. *Why:* Paramour's cold-start problem is local, simultaneous density rather than total account count; manufacturing that density once lets the team validate whether discreet mutual interest produces real in-person conversations without changing the product into speed dating or an events company.*

- **The first launch uses a EUR 10 attendance deposit charged at reservation and refunded after verified arrival.** The guest completes the Paramour profile before reserving, pays through Stripe-hosted Checkout, and may cancel for a full refund until a clearly advertised deadline; an unexcused no-show after that deadline forfeits the deposit. Paramour triggers a full refund after entry verification and explicitly warns that the bank may take several business days to display it. Card processing costs retained on refunded payments are accepted as launch acquisition cost. A later venue partnership may replace refunds with a EUR 10 drink credit, but only after the eligible drink, redemption, settlement, tax, and no-show allocation are agreed with the venue. *Why:* a free registration does not provide credible attendance, while a refundable deposit creates commitment without turning the launch into a paid dating event; charging and refunding is operationally dependable across a longer registration window, unlike a short-lived card authorisation or a more failure-prone off-session no-show charge.*

- **Event entry verification and voluntary room presence are separate actions.** Each paid reservation receives an individual entry QR. A founder or designated greeter scans it in a founder-only mobile surface, verifies the guest summary, and explicitly confirms arrival; that idempotent confirmation records check-in and queues the Stripe refund, with duplicate scans showing the original check-in instead of issuing another refund. The guest still scans the venue's permanent QR or otherwise confirms presence on their own device before becoming visible in Paramour. A manual guest search and founder-only refund correction path cover damaged phones and payment incidents. *Why:* entry verification needs a fast, auditable anti-no-show control, but an organizer must not opt a guest into public venue visibility; separating the two preserves consent, prevents duplicate refunds, and keeps the venue's reusable QR independent from event ticketing.*

- **Stripe is integrated server-side and its webhooks are the payment source of truth.** Paramour never handles or stores raw card details: reservation payment uses Stripe-hosted Checkout, internal records retain only the minimum provider and reservation identifiers/statuses, and authenticated server-side handlers create Checkout sessions, process signed idempotent webhooks, and initiate refunds. Browser success redirects are informative rather than authoritative. Refund work is queued after arrival so venue entry does not depend on Stripe responding synchronously, and founders have an auditable manual recovery control. *Why:* payment state can complete after a browser closes or callbacks can be retried; provider-verified events, idempotency, and asynchronous refunds keep money movement correct without putting secrets or card data in the client or slowing the door queue.*

## 2026-08-31

- **Exhaustive Realtime invalidation for room eligibility changes is tracked separately in #195 rather than broadened inside #118.** The live room continues to update immediately for presence changes, visibility changes, matches, and locally initiated blocks, and it fully resynchronizes after foreground return or socket reconnection. Remote blocks and profile preference edits can make an already-rendered candidate stale without producing a safe client-visible event; solving that requires a privacy-preserving invalidation design with explicit fan-out, debounce, reconnect, and scaling semantics. #118 therefore owns the empty-room product states and the transitions supported by the current event model, while #195 owns prompt convergence for eligibility mutations that lack such an event. *Why:* a naive venue-wide broadcast would make every participant refetch after every private change, adding avoidable load while risking metadata leakage; separating the architecture work keeps the shipped empty state honest without silently committing the product to an indiscriminate Realtime mechanism.*

## 2026-09-01

- **Paramour will not pursue a Progressive Web App for V1.** The Safari-to-Home-Screen session-continuity proof of concept is abandoned and its experimental branch is removed; the existing web experience and Vercel preview flow remain the supported surfaces. *Why:* preserving an anonymous identity across Safari and an installed Home Screen app remained physically unvalidated and would require cookie-backed auth, refresh-chain handling, additional CSRF/CSP work, and cross-platform lifecycle testing. That complexity does not advance the core live, in-person match loop enough to justify carrying it into V1.*

## 2026-09-02

- **Amourette is the current product name everywhere active; older Paramour references in this append-only log are historical language, not current guidance.** Browser and QA identifiers move to the `amourette-` namespace with temporary reads of their legacy values so the cleanup does not reset participant preferences, room hints, unread state, pending chat delivery, or existing synthetic fixtures. The separately tracked `bartap-close-ended-nights` database job remains operationally unchanged until the founder-gated migration in #200. *Why:* removing the retired brand now prevents agents and new code from perpetuating it, while explicit compatibility reads and a separate database change avoid turning a naming cleanup into a participant-state or shared-infrastructure regression.*

- **An empty, active match chat offers three localized in-venue conversation starters above the composer, and the matched profile has a deliberately limited responsive preview from the header (#154).** Starters disappear as soon as a draft or any message exists; explicit dismissal is stored per match in local storage with the match expiry so stale choices clean themselves up. The header preview is a Radix dialog rendered as a mobile bottom sheet or a centered desktop card and exposes only photo, first name, and bio; reporting and blocking remain in the adjacent safety menu. *Why:* fixed, discreet prompts reduce the friction of starting the first real-life contact without turning chat into an engagement product, while a small contextual reminder can help someone approach the right person without expanding profile browsing or weakening the existing safety path.*

- **Large project missions are represented as parent issues with scoped sub-issues, not as parallel task lists or broad implementation tickets.** Parent issues stay in `Backlog` and communicate the outcome and aggregate progress; executable work lives in their independently pickable sub-issues. Project views should expose GitHub's native `Parent issue` and `Sub-issues progress` fields and may filter a specific mission with `parent-issue:OWNER/REPO#NUMBER`. The launch reservation work in #183 is the reference structure, and #35 and #38 now follow it. *Why:* the board needs a readable level between roadmap phases and individual tasks without duplicating the source of truth, while native issue hierarchy keeps ownership, status, PR closure, and progress connected.*

## 2026-09-03

- **Conversation starters are a discreet vertical list with no dismissal control or persisted dismissal state, superseding that part of the 2026-09-02 decision for #154.** The three translucent full-width rows disappear while a draft exists, reappear if it is cleared before sending, and stay gone once the conversation has a message; selecting one only fills and focuses the composer. *Why:* the lightweight list no longer needs a close control, and the visible editable draft is sufficient confirmation before sending, reducing interaction and visual clutter without weakening the first-contact aid.*

- **Conversation-starter rows hug their content and hide while the composer is focused, refining the earlier #154 decision.** An empty composer shows them again only after it loses focus. *Why:* content-width bubbles read as prompts rather than a second form, and hiding them at the first composing intent prevents the mobile keyboard from leaving the stack stranded over the top of the thread.*

## 2026-09-04

- **Opening the room overflow menu locks the profile feed until the menu closes (#97).** The menu remains a short contextual overlay rather than a scrollable surface, and an outside tap closes it before the feed can move again. *Why:* report and block act on the profile currently in view, so allowing the feed to scroll behind the open menu could silently change the target and make a safety action ambiguous.*

- **Profile first names are limited to 30 characters across the UI and database.** Existing room cards preserve their display scale through 18 characters, then step down twice through the limit; compact match and chat surfaces truncate while their action menus expose the complete name. Existing values are never automatically shortened, and the constraint migration must only be applied after checking that no stored profile exceeds the new limit. *Why:* 50-character names can displace core room and safety controls on narrow phones, while 30 characters accommodates real first names and gives every supported surface a predictable, non-overflowing bound.*

- **Consecutive chat messages from the same sender collapse onto one trailing timestamp; a 3-minute gap or a sender change starts a new run (#167).** Only the last message of a run shows its visible `<time>` label; every other message keeps an `sr-only <time datetime>` so the information stays available to assistive tech. A message's own pending/failed delivery status still renders wherever it applies, even mid-run, so the retry action is never hidden by the grouping. The vertical gap between bubbles in the same run is tightened relative to the gap between runs, so the grouping reads visually rather than only through the absent timestamp. *Why:* repeating an identical timestamp under every bubble in a quick exchange adds metadata noise and pushes bubbles apart for no informational gain; 3 minutes matches the common messaging-app convention, short enough that a genuinely later reply is not silently absorbed into an earlier run.*

- **The match chat composer's localized placeholder is removed rather than kept, softened, or shown only until the first message (#167).** An empty, active match already shows three localized conversation starters above the composer (2026-09-02 decision) which model a short, in-person-oriented first message by concrete example; the composer's placeholder text became redundant the moment it reappeared on focus, since the starters had already just demonstrated the "short message" instruction more effectively than a generic label restates it. The thread's own empty-state copy ("No messages yet. Keep it warm, short, and respectful.") continues to carry that guidance for the rare case where starters are not shown (one participant no longer present). *Why:* once the starters cover the teaching job the placeholder existed for, the conditional "hide after first message" option only adds state-tracking complexity for a cue that is already redundant on arrival; this also retires the localized-placeholder part of #100's copy-length review criterion, though the composer's other localized copy (send action, delivery states) remains in scope for it.*

## 2026-09-06

- **Onboarding returns to its original normal-flow layout with no autofocus on the first-name or bio fields (#216).** Remove the experimental fixed viewport shell and keyboard scroll-restoration handlers. *Why:* the bottom-anchored shell adapted from chat caused a reported iPhone regression with a large blank area above the wizard on first-name focus; the chat's thread/composer geometry does not establish the same viewport behavior for onboarding. Removing autofocus avoids an unsolicited focus-triggered scroll at step entry. Any further correction for the residual offset after keyboard dismissal must follow measurements of document scroll, ancestor/container scroll, visual viewport geometry, and element positions on the affected device; the residual cause remains unconfirmed.
